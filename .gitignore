.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
*.egg-info/
.env
*.sqlite3
.DS_Store
**/.DS_Store

# `evidence/` is COMMITTED on purpose - README section 7 lists comparison.csv, report.md and
# predictions.json as graded artifacts. Only the per-window run documents are regenerable noise.
evidence/raw-results/

# large raw media stays out; captions/transcripts/chat are committed
evals/fixtures/**/audio.opus
evals/fixtures/**/raw/

# fixture-farm scaffolding output, not product
.farmlogs/

# capture salt - keeps pseudonyms stable locally, must never be distributed
.capture_salt

# never commit
telegram-export/
*.ogg
