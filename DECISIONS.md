# Decisions

Every scope or technical decision, with a one-line rationale. This file is the raw material for
`docs/IMPROVEMENT_CHANGELOG.md` — write entries as decisions are made, not afterwards.

| Date | Decision | Rationale |
|---|---|---|
| 2026-08-29 | Replay is the graded path; live is demo-only | Live streams are not reproducible; judges cannot re-run a broadcast |
| 2026-08-29 | Content-addressed model-call cache, committed | Lets judges reproduce every number with no API keys and zero cost |
| 2026-08-29 | Two primary metrics instead of a composite GS-F1 | A 4-clause matching rule is unstable on 12 cases and illegible in a 5-minute video |
| 2026-08-29 | Unsupported-card rate as a primary metric | Deterministically checkable with no gold labels, so it scales to every fixture |
| 2026-08-29 | Baseline receives the same raw multimodal events | Comparing against chat-only would measure extra data, not the agentic workflow |
| 2026-08-29 | One agent, bounded tools; no web-search or scheduler agent | Neither addresses a demonstrated failure; PDF states purposeful choices beat component count |
| 2026-08-29 | Dashboard is the landing page | Halves the frontend work and feeds End-to-End Quality instead of competing with it |
| 2026-08-29 | API-only, `deepseek-v4-flash` | No local compute available; `deepseek-chat` retired 2026-07-24 |
| 2026-08-29 | Cache key names media by SHA-256; bytes never enter the entry | The cache is committed for keyless reproduction — a 19 MB WAV or 100 KB of base64 per frame does not belong in git |
| 2026-08-29 | Deepgram via REST + `httpx`, no SDK | `httpx` is already a dependency; an SDK would add a version surface for one POST |
| 2026-08-29 | Frame prompt written fresh, not ported from `legacy/.../image_annotations/workers.py` | The loop's context guardrail allows only three legacy files; the intent (factual, ≤500 chars, "text unclear" over invention) was already recorded in the enrich docstring |
| 2026-08-29 | Enrichment writes rows without `id`; the replay loader derives them | One implementation of the id rule, so the writer and reader cannot drift; `EventIndex` already rejects duplicates |
| 2026-08-29 | Deepgram `utterances` preferred, word-grouping only as fallback | Deepgram's own final segmentation beats a hand-tuned pause threshold, and the fallback keeps enrichment working if the flag is ever dropped |
| 2026-08-29 | Analysis windows are fixed 60 s tiles derived from the fixture span | Window bounds enter the prompt; tiling on event volume or wall-clock would change every cache key and break replay reproduction |
| 2026-08-29 | Agent and baseline run through one `_run_over_fixture` on identical windows | The eval compares them — any difference other than the system itself would be measured as an improvement it did not earn |
| 2026-08-29 | The CLI reports counts, never rates | `evals/scorer.py` owns every published metric; a second implementation would eventually disagree with it in print |
| 2026-08-29 | `trace_id` derived from `(agent, case_id)`, not `uuid4` | Unseeded randomness leaked into the published result document, so two replays of one fixture differed and each run orphaned a trajectory file |
| 2026-08-29 | `serve` uses stdlib `http.server`, not the declared FastAPI/uvicorn | `make demo` is off the graded path; stdlib keeps the fresh-clone install smaller and has no startup surface |
| 2026-08-29 | `serve` never runs the agent — it only serves what `make replay` wrote | What a judge sees on screen is then exactly the file they can open and diff |
| 2026-08-30 | The eval scores every card a system emits, verified and rejected alike | Scoring only gate survivors makes the unsupported-card rate zero for both systems by construction — the headline metric would measure nothing |
| 2026-08-30 | `latency_ms` and `cost_usd` stay `None` in `comparison.csv` | A replay run reads cached responses, so timing it measures disk, not the model; publishing that as latency would be a fabricated number. Cost lives in `COST_LEDGER.md` |
| 2026-08-30 | Chat-only ablation is opt-in (`--ablation`), not part of the default eval | It is a diagnostic, not the headline comparison, and every extra system is 12 more paid calls in the record phase |
| 2026-08-30 | `evidence/predictions.json` written on every run | Frozen protocol item 4 — raw predictions, gate decisions and trace id per case, failures included, not only the wins |
| 2026-08-30 | Only 4 eval cases built; the other 8 wait for real captures | The scaffold fixture contains three phenomena in 24 s. Authoring a fixture to contain sarcasm or a diarization failure and then grading on it would be grading the system against a script it was handed |
| 2026-08-30 | Every case declares `fixture_kind`; only `capture` is reportable | Without it a pipeline smoke-run on synthetic data silently becomes a published number — the exact integrity failure the legacy frontend committed |
| 2026-08-30 | `report.md` banners non-capture fixtures and lists every fixture's provenance | A judge opens `evidence/report.md` first; the table has to say what it is measuring before it shows a number |
| 2026-08-30 | Case 12's abstention window has a frame caption but zero chat | An empty window is a trivial test; stream context with a silent audience tests the real failure — inventing a signal out of context alone |
| 2026-08-30 | The debrief takes only gate-verified cards | It is what a streamer reads after the broadcast; a card whose evidence did not check out has no business in it. The eval scores rejected cards too, but that is measurement, not the product |
| 2026-08-30 | Recurring themes count words from trigger quotes and answer distributions, never card titles | Titles are prose the model wrote — the first version of the section returned "chat", "says" and "the", i.e. it measured the summariser's phrasing rather than the session |
| 2026-08-30 | Themes use a twelve-word visible stopword list, not a package or a model call | Extendable from observed noise, auditable at a glance, and it cannot change a cache key |
| 2026-08-30 | Clip candidates exclude cards whose trigger is `unknown` | Density alone is not a clip: with no established cause there is no moment to cut to |
| 2026-08-30 | Timecodes resolve through the triggering event, falling back to the window start and saying so | A debrief that prints a confident timestamp derived from a 60 s bucket is quietly wrong; the label makes the precision visible |
| 2026-08-30 | Dashboard is written fresh, nothing ported from `legacy/frontend/` | The port would have carried the generators, the Spline blob and the purple gradients — the exact things DESIGN.md says to delete — and the loop forbids reading that tree at all |
| 2026-08-30 | No CDN, no webfont fetch, no build step; vanilla JS in three static files | A judge with no connectivity must still see the real dashboard, and a build step is another way a fresh clone fails to run |
| 2026-08-30 | Status vocabulary is verified / abstained / rejected, not DESIGN.md's "uncertain" | A card whose evidence did not check out is not uncertain; calling it that would soften the one failure mode this product exists to surface |
| 2026-08-30 | Rejected cards are rendered on the page, in their own labelled block | A system that hides its own rejects cannot be audited, and the gate working is the strongest thing the demo has to show |
| 2026-08-30 | `/api/replay` sends only the events the cards cite, not the fixture | The drawer needs message text; shipping a whole fixture to the browser is wasteful and would let the page display chat no card stood behind |
| 2026-08-30 | Colour conformance is a test, not a convention | The rule "do not improvise colours" is the one the previous shell broke; every hex in `app.css` must appear in `DESIGN.md` or the build fails |
| 2026-08-30 | `pyproject.toml` with a src layout; `make setup` installs the package editable | Every documented command failed from a clean clone with `ModuleNotFoundError`, and reproducibility is a gate before scoring — a project that cannot start is never read |
| 2026-08-30 | Runtime dependencies stay in `requirements.txt`, not in `pyproject.toml` | They are pinned and split into replay / live-capture / dev groups; declaring them twice is how the two lists drift apart |
| 2026-08-30 | The README leads with what is NOT measured yet | A reader who finds the caveat after the results table has already been misled once; the exit-3 behaviour is a strength and reads as one when stated first |
| 2026-08-30 | Twitch retention and Jan-2026 cost figures kept out of the README | They come from planning notes rather than a source. Recorded as UNVERIFIED in `RISKS.md`; the README makes the same points qualitatively |
| 2026-08-30 | The documented test count is asserted by a test | It is the first number a reviewer checks, and it silently drifted by nine within one iteration of being written down |
| 2026-08-30 | Documented `make` targets are checked against the Makefile by a test | A reproduction guide naming a target that does not exist fails the reviewer at the step that decides whether the project is scored at all |
| 2026-08-30 | Secret scan moved from a Makefile grep to `scripts/scan_secrets.py` | The grep never reached `.env` on macOS, matched its own pattern list so the check was always red, and skipped `legacy/` while `legacy/` stayed in the tree — three ways to be reassured by nothing |
| 2026-08-30 | The scan prints `path:line` and a rule name, never the matched text | A gate that echoes credentials into a terminal, a CI log or a transcript has moved the leak rather than found it |
| 2026-08-30 | Two severities: leak in a project file vs. local-only file present | `.env` existing is correct on a working machine and fatal in an archive; collapsing the two makes the operator ignore both |
| 2026-08-30 | DB user, host and name count as credentials alongside the password | They are one connection block. The first pass fired only on the password line and left the rest of `legacy/README.original.md` unreported |
| 2026-08-30 | `legacy/README.original.md` left unmodified despite holding live credentials | `docs/PRE_EXISTING.md` states `legacy/` is preserved as-is; rotating and excluding it from the archive is the author's call, and silently editing a disclosure artifact is worse than flagging it |
| 2026-08-30 | `TS_TRACE_DIR` redirects trajectory output, set session-wide in conftest | The suite wrote 55 files into a graded deliverable; redirecting once fixes the class of problem instead of the instances, and mirrors how `TS_LLM_MODE` already works |
| 2026-08-30 | The 55 test-artifact traces were deleted rather than left in place | They are regenerable output with case ids no evaluation case has; leaving them means the deliverable directory misrepresents what was run |
| 2026-08-30 | `trajectories/README.md` lists all three systems as `[TBD]` rather than only what exists | Three systems are compared, so three trajectories are owed; a README that lists only the ones that exist reads as complete when it is not |
| 2026-08-30 | The coding-agent disclosure lists what the sessions found, not only what they built | A disclosure that claims productivity and omits the defects the agent introduced or missed is not a disclosure |
| 2026-08-30 | No chat transcript is claimed; `PROGRESS.md` and `DECISIONS.md` are named as the record | They were written as the work happened. Producing a transcript after the fact would be exactly the reconstruction the trajectory rules forbid |
| 2026-08-30 | `.env` is deliberately not auto-loaded; keys are exported instead | `.env` here also sets `TS_LLM_MODE`, so `load_dotenv()` would let a local file silently flip a reviewer's replay run into live or record mode — the reproducibility guarantee would depend on a file nobody sees |
| 2026-08-30 | `RISKS.md` reordered by severity with a critical-path header, numbers kept stable | It had become a log. A reviewer needs to know what to do first and what everything else is waiting on; `PROGRESS.md` refers to the numbers, so rows move but never renumber |
| 2026-08-30 | Superseded risks are kept and marked rather than deleted | #9 split into #14 and #15; deleting the row would erase why the policy exists |
| 2026-08-30 | Six unused dependencies removed, plus `pytest-asyncio` | An AST census found no import of any; each was install time and supply surface on the path a judge walks before the project is scored. `mypy` stayed — it is run as a command, not imported |
| 2026-08-30 | The reduction was proved in a clean venv, not asserted from the working one | The packages were still installed locally, so a green suite here would have proved nothing about their necessity |
| 2026-08-30 | A test fails the build when a declared package is never imported | Dead dependencies come back one convenience import at a time; the census that found these six should not have to be run by hand again |
| 2026-08-30 | The keyless guarantee is asserted by executing the chain with sockets rigged to raise | Reading the code and concluding it looks offline is how the secret scan was trusted for a week; the property is cheap to actually run |
| 2026-08-30 | The fake provider is installed only around the record step, never during replay | If the fake stayed in place, a replay that tried to call out would be caught by the fake instead of by the socket trap, and the test would prove nothing |
| 2026-08-30 | C2 split into the part that needs fixtures and the part that does not | The keyless property is checkable today on the scaffold; waiting for #2 to check any of it would have left the gate criterion unverified all night |
| 2026-08-30 | `capture_media` refuses to run into a directory that already holds frames | A re-run stamped already-timestamped frames from zero against a new start time, silently corrupting every frame timestamp — and capture is precisely the step that gets retried after it fails |
| 2026-08-30 | A capture producing no frames or no audio raises instead of returning | It returned success and wrote a `meta.json` declaring a good fixture; the emptiness would have surfaced at enrichment, hours later, with the broadcast over |
| 2026-08-30 | ffmpeg's return code is checked and its stderr surfaced | Nothing inspected it, so a missing binary or a bad argument produced an empty capture that looked like a quiet stream |
| 2026-08-30 | `stamp_frames` extracted from `capture_media` and tested directly | The timestamp arithmetic is the contract `enrich` depends on, and it was reachable only through streamlink and ffmpeg |
| 2026-08-30 | A test drives capture → enrich → `load_fixture` across the real seam | That join decides whether a recorded fixture is usable at all, and it would otherwise be discovered at 5am |
| 2026-08-30 | The architecture diagram marks every node with the file that implements it | A diagram showing a design rather than a build is worth nothing to a reviewer who opens `src/` — and it is how a summary hierarchy nobody wrote ended up claimed in two documents |
| 2026-08-30 | The unbuilt summary hierarchy is listed and marked, not quietly dropped | Dropping it makes the design look smaller than it is; listing it unmarked claims work that does not exist. Naming it as a gap does neither |
| 2026-08-30 | The architecture diagram now includes capture, enrich, the cache and the eval harness | The previous one omitted exactly the parts carrying Reproducibility and Measured Improvement — 30 of the 100 points |
| 2026-08-30 | Documented `make` targets are matched only inside code spans | The prose "make the design look smaller" was read as a target named `the`; a guard that fires on English gets disabled rather than fixed |
| 2026-08-30 | The approve action is a real button producing a real draft, not a status label | It is the last link of the product invariant. A card reading "pending approval" with nothing to press is a chain that ends in a caption |
| 2026-08-30 | The draft is built server-side in `report/poll.py`, not assembled in the browser | What a streamer approves has to be derived deterministically from verified evidence, and that belongs somewhere testable |
| 2026-08-30 | `poll.py` contains no client, token or request, and a test enforces that | Ground rule 06 keeps polls as drafts; the cheapest way to keep it true is for the code path to hold nothing capable of sending |
| 2026-08-30 | Poll shares count every vote, not only the options that survived the cap | Renormalising would print a percentage disagreeing with the card directly above it on the page |
| 2026-08-30 | A card with fewer than two counted options does not become a poll | A poll with one choice is not a poll, and inventing a second option would be fabricating audience input |
| 2026-08-30 | A draft from an abstained card uses the card title and says so | Without an established cause there is no quote to ask; presenting the title as the streamer's own words would be the invention the gate exists to prevent |
| 2026-08-30 | A `none` card is checked for self-contradiction, not for evidence | It asserts nothing, so there is nothing to verify; requiring citations from a card that claims nothing made correct abstention score worst on the headline metric |
| 2026-08-30 | An abstention citing messages or naming a cause is a violation | Those are the only ways it can be wrong: it would be claiming something while declaring that nothing happened |
| 2026-08-30 | Passing abstentions are not free — the other metrics still catch over-abstention | Signal recall and abstention correctness both punish abstaining where a signal existed, so the gate does not need to double as that check |
| 2026-08-30 | A passing `none` card is labelled `abstained`, not `verified` | It is not a verified signal, and the dashboard and debrief already use that word |
| 2026-08-30 | The gate's window is half-open `[start, end)`, matching `events.window` | Two conventions in one system means the gate verifies evidence the agent's own tools never returned; adjacent tiles make that a boundary case on every window, not an edge case |
| 2026-08-30 | A test asserts every frozen gold card passes the gate | Gold labels describe what a perfect system emits. If a tightening invalidates the frozen set, that has to surface on the same commit, not in the results table |
| 2026-08-30 | Evidence must be a `chat_message`; any other event type is a violation | The gate accepted any id, so a card could cite the transcript segment that caused the signal as the audience's response to it — self-supporting and verified |
| 2026-08-30 | The trigger appearing in its own evidence list is a violation in its own right | The type check already prevents the common case, but stating the circularity rule explicitly is two lines and says what the gate means |
| 2026-08-30 | The short-quote hole is documented, not fixed | The frozen metric definition says "does not appear verbatim"; tightening it after publication would make the reported numbers incomparable to the metric they claim to be. Recorded in README §11 and RISKS #28 |
| 2026-08-30 | Punctuation is stripped by Unicode category, keeping symbols | `[^\w\s]+` deleted emoji along with punctuation, so an emote flood — the reaction the product most needs to catch — reduced to nothing and disappeared |
| 2026-08-30 | A punctuation-only message goes to a counted `∅` bucket rather than being dropped | Burst counts have to sum to the messages in the window; anything dropped is volume the agent never sees and a distribution that quietly under-counts |
| 2026-08-30 | The emote fix was checked against the frozen fixture before being kept | It reduces identically — 32 → 15 bursts, ratio 0.469 — so no cached prompt or frozen case moves. A correctness fix that shifts frozen numbers is a different decision and would need saying out loud |
| 2026-08-30 | Trigger accuracy keeps a matched-card denominator, and `unmatched_rate` is reported beside it | Gold is not exhaustive on twelve cases, so scoring every emitted card would penalise a real signal nobody labelled. The honest fix is to publish what is computed and expose the hole next to it, not to redefine the metric to sound better |
| 2026-08-30 | Gold matching is one-to-one, assigned in emission order | A gold signal can be found once. Without it a duplicate card weighted one signal twice, and emission order keeps the assignment reproducible |
| 2026-08-30 | The README now describes the metric the code computes, with the probe that exposed the gap | A published definition the implementation does not honour is worse than a narrower metric honestly described |
| 2026-08-30 | The card cap is enforced by the controller, in one function shared by both systems | A cap that lives only in the prompt is a hope, and the eval promises both systems the same one — measured, ten cards in gave ten cards out, for both |
| 2026-08-30 | Unknown card types are filtered before the cap is applied | Otherwise five junk cards fill the cap and squeeze out the real ones |
| 2026-08-30 | Dropped cards and dropped tool calls are both reported, never silent | A silently truncated tool-call list is indistinguishable to the model from a tool that returned nothing, and a silently dropped card is a claim the system made and hid |
| 2026-08-30 | Eval set rebuilt on real captures; the four synthetic cases deleted | A case on the scaffold fixture is bannered NOT A REPORTED RESULT, so it can never contribute a graded number |
| 2026-08-30 | 11 cases, not 12 | The cut case's window sat 72% inside c05's on a 120 s fixture; two cases sharing most of their events are not two independent measurements |
| 2026-08-30 | No numeric-rating or binary-choice case | Zero bare-number replies exist in any fixture and the one real binary question lands 8 s before its fixture ends. Authoring a fixture to contain the phenomenon would be grading against a script |
| 2026-08-30 | Gold evidence ids resolved from the fixture by script, never typed | A mistyped id is not an error, it is a silent miss that scores a correct card as wrong and quietly deflates every metric |
| 2026-08-30 | Gold signals are pushed through the real provenance gate in a test | Gold that cannot itself pass the gate makes the whole eval unmeasurable; the failure is invisible at run time |
| 2026-08-30 | Gold marked `reviewed: false` and surfaced in README §6 | The labels are model-drafted. Claiming human ground truth that does not exist is the one unrecoverable mistake in an evaluation |
| 2026-08-30 | Runner tests own their case data via a `stub_cases` fixture | Ten tests about the eval harness broke when the graded case set changed; the harness must be testable independently of the deliverable it scores |
| 2026-08-30 | Git history disclosed as reconstructed in README and PRE_EXISTING | The tree was never under version control, so commit dates are assigned. Letting a judge read `git log` as observed history would be a fabricated timeline |
| 2026-08-30 | **repair** — baseline gets its own system prompt; the card contract is one shared string | It was being handed the agent's tool-calling protocol, replied `{"action":"call_tools"}`, and `.get("cards", [])` turned that into a clean zero across all 11 cases. Contract is byte-identical for both systems; only the route to an answer differs, which is the thing being measured |
| 2026-08-30 | **repair** — the contract now states that a trigger is a speech or screen id, never a chat id, and never also in evidence | 19 of 20 agent cards were rejected on E_CIRCULAR_EVIDENCE alone. The gate has always enforced this; the prompt never said it. A scorer may not penalise a rule the contract never expressed. This does raise the agent's score, so note it went verbatim to the baseline too |
| 2026-08-30 | **repair** — a reply parsing to no `cards` key is a recorded failure, not an empty result | Conflating the two is exactly what let eleven zero-card cases print as a result. `{"cards": []}` stays a legitimate answer |
| 2026-08-30 | **repair** — `make eval` banners any system that emitted zero cards and exits 5 | `trigger_accuracy: null` reads as 'not computed yet' when it means 'broken'. Fourth instance of the silent-success class in this project |
| 2026-08-30 | `evidence/` is committed; only `evidence/raw-results/` is ignored | A .gitignore rule added on 30 Aug would have excluded comparison.csv, report.md and predictions.json — the artifacts README section 7 promises a judge |
| 2026-08-30 | Rejected: tuning either prompt after seeing the numbers | Out of scope by the working rule. No entry in this file is labelled `tuning` |
| 2026-08-30 | **repair** — `render_events` labels ids as `id=` instead of leading with a bracketed timestamp | Measured: every system cited the timestamp as the event id and was rejected on E_UNKNOWN_MSG, so no card could match gold or clear the gate. The agent reads ids from JSON tool results and was unaffected, so this repair strengthens the BASELINE only |
| 2026-08-30 | **repair** — `DEFAULT_TEXT_MODEL` is the model the cache was recorded with, not an aspirational one | The model is part of the cache key. `make eval` reproduced only for an environment that set `TS_TEXT_MODEL`; a judge's fresh clone missed every entry and exited 3. Reproducibility is a pre-scoring gate |
| 2026-08-30 | Model disclosure corrected to gpt-4.1-nano / gpt-4.1-mini | README named `deepseek-v4-flash`, which no recorded run ever used. A disclosure has to name the model actually called |
| 2026-08-30 | Rejected: making the agent call `get_frame_captions` before it may abstain | Out of scope — tuning. The agent returned `none` on the word-puzzle case claiming 'no on-screen content' having never queried the screen. That is the measured result and the material for the hot take, not a prompt to edit after seeing the score |
| 2026-08-30 | Rejected: broadening gold `relevant_message_ids` to whole bursts | Out of scope — editing the instrument after seeing results. Recorded as a limitation instead: gold lists the first 5 ids per burst, so a card citing a different member of the same burst scores unmatched, which understates recall for every system |
| 2026-08-30 | Report the agent losing the headline metric rather than tuning to win it | The agent's unsupported rate is 0.739 against the baseline's 0.600. An honest loss with a diagnosis scores on Measured Improvement and Hot Take; a swept comparison risks the integrity gate, which is worth more than both |
| 2026-08-30 | The hot take is the ablation result, not a pitch line | The chat-only system won the headline metric by abstaining, because it had no candidate causes to name. That is a real finding about metric B and about the problem, and it came out of the measurement rather than being decided in advance |
| 2026-08-30 | No per-component ablation claimed for the reducer, tools or gate | There was time for one honest end-to-end comparison, not five. Claiming component-level deltas that were never run would be inventing measurements |
| 2026-08-30 | README §6 does not say gold was 'reviewed by the author' | No human has read the labels; every gold file says `reviewed: false`. The sentence was in the working brief, but writing it would have claimed a review that did not happen |
| 2026-08-30 | Capture-only dependencies split into `requirements-record.txt` | `streamlink` needs Python 3.10+ and no graded command imports it, yet `make setup` installed it — so a clean clone on macOS system Python 3.9 failed at the first documented command, before the test suite ran. Reproducibility is scored before anything else |
| 2026-08-30 | `make setup` checks the interpreter version and names a fix | The failure was a pip dependency-resolver dump. A reviewer reads that as 'this project is broken', not 'use a newer python' |
| 2026-08-30 | Recorded the tiled windows for two fixtures | `make replay`/`make baseline`/`make debrief`/`make demo` are documented commands that exited 3, because the cache held only the frozen case windows. $0.03 to make every documented command work from cache |
| 2026-08-30 | **repair** — `cap_cards` ignores non-dict entries; the debrief ignores a non-mapping `distribution` | Both crashed a run on real model output. A malformed reply is a bad answer, not an exception, and the crash in `cap_cards` happened mid-record after earlier windows had already been paid for |
| 2026-08-30 | Scanner recognises placeholder values (RISKS #35 closed) | Six lines of `DB_PASSWORD=your_password` in a docs example outranked eight live credentials for a day. A scanner that cries wolf on its own README gets switched off — the exact failure the #18 rewrite already cost once |
| 2026-08-30 | `make archive` packages `git archive HEAD`, never the working tree | The working tree holds `.env`, `.venv` and 73 MB of raw audio. `git archive` cannot include an untracked file by accident, which is the property that matters — `.gitignore` does not protect a directory that is zipped rather than committed |
| 2026-08-30 | The archive is opened and re-scanned as part of building it | Building an archive and not looking inside is how the `.env` in #17 would have shipped. The target unzips and runs `scan_secrets.py` against the extracted copy |
| 2026-08-30 | Risks are reviewed by executing their claims | The header still said 'no fixture has been recorded' after four were captured, and #21 still said the recorder had never run after it had run four times. A register reviewed by re-reading itself drifts from the tree it describes |
| 2026-08-30 | `make eval` now includes `--ablation` | It has to reproduce the committed `evidence/report.md` exactly. It did not: running the documented command rewrote the three-row table the README quotes with two rows. The opt-in rationale was record-phase cost, and on replay there is none |
| 2026-08-30 | A repair that moved a published number is declared, not quietly re-run | Repair 5 took the baseline from 20 to 21 cards and 0.600 to 0.619 unsupported. It narrows the gap against us, which is the only direction a post-hoc repair may move a result without challenge — and it is in the changelog either way |
| 2026-08-30 | The poll-draft beat is cut from the video rather than staged | No recorded card carries a `distribution`, so the button never appears. Hand-writing a card to film it would be presenting generated data as real output |
| 2026-08-30 | The shot list scripts the sentences that count against the product | 19 of 24 agent cards are rejected on camera. Showing that and narrating it as success would be the dishonest cut; the script says to name it and return to it in the hot take |
| 2026-08-30 | `make eval` writes `evidence/summary.json`; the dashboard reads it | The editorial sections need the measured numbers, and the alternatives were hard-coding them into HTML (drifts, and is exactly the fabricated-data failure of #4) or recomputing rates in JavaScript (a second implementation that would eventually disagree with `report.md`) |
| 2026-08-30 | The measured section is hidden when no eval has run | An empty table renders as a row of zeros, which reads as a measured result rather than an absent one |
| 2026-08-30 | Tests assert the editorial copy keeps the unflattering sentences | The page states that the agent has the worst unsupported rate of the three and that the ablation's accuracy rests on one matched card. Without a test those are the first sentences to soften before a demo |
| 2026-08-30 | Published numbers are asserted against `evidence/summary.json`, not maintained by hand | The table lives in five documents and had been hand-propagated twice. A stale number still reads plausibly and nothing fails, which is the only kind of error this project cannot afford |
| 2026-08-30 | Every consistency check carries a vacuity guard | The first version of this one silently matched two of three systems because of a footnote marker. A check that stops checking is worse than no check — it is the failure class of the old Makefile grep scan and the pre-rewrite secret scan |
| 2026-08-30 | `legacy/frontend/` deleted from the tree, not just excluded from the archive | It renders fabricated names, emotes and cluster values from `Math.random`. An `export-ignore` would keep it out of the zip and leave it in a public repository — half a fix for the one integrity risk this submission cannot absorb. It stays in git history and stays disclosed in `docs/PRE_EXISTING.md` |
| 2026-08-30 | Packaging guarantees are asserted by test, not by policy | `.env` untracked, `git archive HEAD` structurally unable to include it, `.gitattributes` export-ignore as a third layer. RISKS #17 turned on a rule that was true only by accident; a test makes it true on purpose |
| 2026-08-30 | The fixture farm lives in `scripts/` and is `export-ignore`d | It is scaffolding for finding one specific missing phenomenon, not product. It is not in `src/`, not in the architecture diagram, and is not claimed as a feature |
| 2026-08-30 | Block B's shared-spec change was checked, found already applied, and not re-made | The rule it proposes adding is in `CARD_CONTRACT` since iteration 23, verified in the cache: 129 of 173 recorded requests carry it. Writing it again in different words would be a change chosen after seeing the score |
| 2026-08-30 | Rejected: enforcing the trigger rule in the controller like `cap_cards` | Consistent with iteration 21's principle that a prompt rule is a hope until the controller enforces it, and still tuning here. The agent names a chat trigger 14 times to the baseline's once, so a symmetric guard would be asymmetric in effect, and it would lower the agent's headline metric after that metric was published |
| 2026-08-30 | RISKS #38 narrowed from contract defect to model behaviour | All eight failures violate two explicitly stated clauses, and three contradict a third by setting `kind: "unknown"` beside a concrete id. The spec is not the problem; `gpt-4.1-nano` following it is |
| 2026-08-30 | Reference fetched from the npm tarball rather than via `npx getdesign` | No Node toolchain exists on this machine. The tarball is the same artifact the CLI installs, so the reconciliation is against the real published reference rather than a remembered one |
| 2026-08-30 | DESIGN.md kept unchanged after reconciliation; provenance recorded instead | All 19 colour tokens and every type row match the reference exactly, in both directions. The honest outcome of a reconciliation that finds no difference is a dated provenance note, not an edit |
| 2026-08-30 | Hero moved from the 48px display-xl row to the 64px display-mega row | The scale was documented and not used. Both files already said 64px/300/1.05/-1.92px for a hero; the stylesheet was rendering the subsidiary row |
| 2026-08-30 | Uppercase labels pinned to the caption-uppercase row by test | The two I added in the previous iteration improvised .8px tracking with no weight, so they silently disagreed with the badges beside them. Type drifts one plausible value at a time |
| 2026-08-30 | Dashboard flipped from a dark canvas to the light editorial system | DESIGN.md and the fetched reference both describe off-white canvas, white cards and ink type; the stylesheet had implemented a dark developer-tools canvas neither asked for. The reference's own description rules it out explicitly |
| 2026-08-30 | Signal status conveyed by label, weight and hairline — never colour | A colour-coded status is read at a glance and never opened. The product's whole argument is that a claim is worth nothing until you look at its evidence, so the UI must not offer a shortcut past it. `--success` and `--error` are now unused |
| 2026-08-30 | Pastel orbs are atmosphere with a test to keep them that way | Fixed, blurred, low-opacity, behind content, meaningless. A test bounds the opacity and forbids an orb token appearing outside the `.orb` rules, so they cannot drift into being a state or chart colour |
| 2026-08-30 | The shadow test was tightened, not weakened, to admit the documented tier | It forbade `box-shadow` entirely while DESIGN.md defines `--shadow-hover` as the ONLY tier for hovered cards. It now checks the value matches DESIGN.md, that every use is on a `:hover` selector, and that exactly one tier exists |
| 2026-08-30 | The hero stage renders nothing when a run verified nothing | It is the product's argument, played with real cited messages and real cards. With no verified card there is no argument, and animating one would be presenting generated data as real output |
| 2026-08-30 | Rejected the ui-ux-pro-max `--design-system` palette and typography | It returned a blue/amber Enterprise Gateway with Fira Code, which breaks three enforced rules at once: saturated accent, display weight, warm neutrals. The skill's own contract is to verify fit before applying; a generated recommendation does not outrank a locked design system |
| 2026-08-30 | Kept the same skill's accessibility and glassmorphism guidance | Both were on target and both exposed real gaps — the stylesheet had no focus styles at all, and the glass had no documented blur band. Taking the useful half of a mismatched result is better than taking all of it or none |
| 2026-08-30 | `--glass-*` tokens added to DESIGN.md before use | The rule is that a design needing a value adds the token first. Glass is translucent white carrying no hue, so the palette is unchanged and text keeps the contrast it already had |
| 2026-08-30 | Glass restricted by test to the sticky bar and the stage header | Frosted surfaces everywhere is a gimmick. It earns its place only where a surface overlaps scrolling content and must stay legible without becoming an opaque panel |
| 2026-08-30 | The hero is pinned server-side to one provable window, not to the loaded fixture | It was rendering whatever the fixture gave it, which on most fixtures is cards with no established cause — under a headline about causation. The pinned window has zero speech and an on-screen word game, so the cause is provably in the frame and nowhere else |
| 2026-08-30 | One grounded card in the hero, never three ungrounded ones | Three cards reading 'Chat mention of X' are echoes, not signals. A card that cannot name its cause cannot carry an argument about causation |
| 2026-08-30 | The hero names the system that produced its card | The pinned card comes from the single-prompt baseline, not the agent. Showing it unlabelled under the product's own headline would be a quiet misrepresentation of which system did the work |
| 2026-08-30 | No chart below two distinct non-zero buckets | A single bucket rendered as a column of indices, empty bars and a vertical one-character label. A chart with nothing to compare is worse than no chart; the fact is printed as a line instead |
| 2026-08-30 | Section 01 renamed and abstentions split out | The heading read 'Verified audience signals' over cards badged ABSTAINED. A heading may not claim what its contents deny |
| 2026-08-30 | Wordmark is a filled dot bound to an outlined dot | The mark should mean what the product means: signal bound to its cause. Ten lines of SVG, used in the nav and the favicon, and available as the video's closing frame |
| 2026-08-30 | Default fixture NOT changed; the picker switches system instead | Measured: the agent grounds zero cards on all three fixtures, so no default fixes the empty flagship section. yugi is worse than stableronaldo for the agent, not better. Letting a judge switch to the baseline turns that empty state into the measured comparison rather than an apology |
| 2026-08-30 | $0.02 spent recording marlon tiles to test the hypothesis rather than assume it | The brief asserted a fixture change would fix the empty section. Checking cost two cents and produced a fact — 0 grounded on a third fixture — that changed the design |
| 2026-08-30 | Three outcome sections collapsed into one segmented control | Grounded, abstained and rejected are one thing in three states. Three full-length stacked sections is what made the page endless, and the counts on one line say more than three headings |
| 2026-08-30 | The fixture picker is over recordings and says so on the page | A control that looks live claims a capability a judge cannot verify and that costs money per use. The note under it names what it is, and unknown input lists what exists instead of erroring |
| 2026-08-30 | Fixture names from the query string go through `Path(...).name` | A filename off a URL is untrusted input. It must resolve beside the served fixture or fall back, verified by requesting a traversal path |
| 2026-08-30 | The front page is the product; evidence moved to `/method` | The page was a report about the system. Playing a recording at its true cadence demonstrates the system instead, and the evidence story keeps every sentence it had — it stops being the front page, it does not stop existing |
| 2026-08-30 | Playback cadence comes from the fixture's own timestamps | 'Looks live' is only honest if the timing is the timing the messages actually had. Nothing is invented, nothing is smoothed, and the badge reads REPLAY from the server's own mode |
| 2026-08-30 | Cards land when their window CLOSED, not when it opened | The window's end is the first instant the agent could have produced them. Emitting at the start would show the answer before the evidence that justifies it |
| 2026-08-30 | Server switched to `ThreadingHTTPServer` | An SSE connection is held open for the whole playback; on a single-threaded server the page itself would never load while a stream was running |
| 2026-08-30 | The opening SSE frame is `meta`, not `open` | EventSource reserves `open` for its own connection event, so a custom listener for it never fires — the badge would have silently kept its placeholder text while claiming to echo the server |
| 2026-08-30 | Phase 2 (true live) not started | It needs API keys and spends money per minute, and the brief gates it behind Phase 1 shipping. Replay stays the keyless default a judge reproduces, which is the property the whole submission rests on |
| 2026-08-30 | The product page is fixed at viewport height; only the columns scroll | The chat is an unbounded stream. Growing the page pushed the counter row off screen, which is the one row that says what the flood turned into — the payoff cannot be the first thing to disappear |
| 2026-08-30 | An empty signals column states when the first card is due | Analysis windows are 60 seconds, so nothing can appear before one closes. A blank panel for that minute reads as broken; naming the wait turns it into anticipation, and a run with no cards says so rather than waiting forever |
| 2026-08-30 | Guards read declarations, not comments | Three separate checks have now fired on a comment naming the thing they forbid. A guard that trips on its own documentation gets disabled rather than fixed, which is how the project lost `make scan` once already |
| 2026-08-30 | The waiting state is a child of the signals column, not a sibling | As a sibling of a `flex: 1` container it was pushed to the bottom of the panel. The message that explains a minute of emptiness has to be where the reader is already looking |
| 2026-08-30 | The chat tint is a bleed layer, not a background on the column | A bounded grey rectangle reads as a widget and re-introduces the container the two-column layout replaced. The tint now extends past the column so it has no corner — a field with a hairline, not a box |
| 2026-08-30 | Chat author ids are right-aligned against the message column | At speed the eye needs one hard left edge to read down. Left-aligned ids of varying width made every message start at a different x |
| 2026-08-30 | The product page is a dashboard; DESIGN.md's editorial density does not apply to it | The token system is a marketing surface — airy, hairline-only. A streamer scanning for signal needs edges and density. Colours, type scale and weight rules are unchanged; spacing, borders and structure are not |
| 2026-08-30 | Line weight given a hierarchy: `hairline-strong` structural, lighter tiers in-panel | Everything was drawn at one weight, so nothing separated from anything. A uniform rule is the same as no rule |
| 2026-08-30 | **Reversed** the 'field, not a box' decision after seeing it rendered | It was argued from a principle — a box re-introduces the container — and the principle was wrong for an operator surface. The test now asserts the opposite, with the reversal written down rather than the old test quietly deleted |
| 2026-08-30 | Grounding still not 'fixed' in the agent | It grounds nothing on any fixture because when it answers after `group_repeated` alone, the only ids in its context are chat ids. The remedy is a tool-selection change — RISKS #39, out of scope, and chosen after seeing the score. The finding is surfaced in the UI instead of engineered away |
| 2026-08-30 | The signals header counts what the panel shows, with the split beside it | It read 'Grounded signals · 0' above five visible cards. Second instance of a heading denying its own contents; the first was on the method page |
| 2026-08-30 | A run that grounds nothing prints that, with one click to a system that does | An empty panel reads as loading or broken. Naming the measured result turns the product's weakest number into the thing the page is most honest about |
| 2026-08-31 | Every render target is cleared before it is filled, via one helper | Three functions appended into containers they never emptied. Switching fixture stacked a fresh copy under the last, and only one of the three was visible enough to be reported |
| 2026-08-31 | The badge states how the page is served, not how the run was captured | It rendered `result.mode`, which is `record` in two of three run documents, so a page that only reads committed files displayed RECORD. The captured mode is provenance and now sits in the debug panel |
| 2026-08-31 | Live mode ships, but replay stays the default and the documented path | A judge reproduces this with no keys, and that property is the whole reproducibility claim. Live is reached by an explicit click, never on load, and returns the badge to REPLAY when it stops |
| 2026-08-31 | Live records into a temporary cache, never the committed one | `cache/llm/` is the reproduction artifact. Live windows are keyed on audio bytes that will never recur, so recording into it would grow the artifact with entries no replay can ever hit. Verified unchanged across a live run |
| 2026-08-31 | Live traces go to a temp directory, not `trajectories/` | The first live session wrote one into the graded deliverable and the iteration-9 guard caught it. A demo run is not an evaluation run |
| 2026-08-31 | The live badge states the lag rather than implying real time | A 60-second window cannot be analysed until it has finished; enrichment and the agent add more. Roughly 90 s behind, said on screen |
| 2026-08-31 | `/philosophy` states the result that argues against the product | The chat-only ablation winning the headline metric is the most interesting number in the repository. A page about the product's reasoning that omitted it would be marketing |
| 2026-08-31 | Shipping tests answer 'what reaches a judge' in both a checkout and an archive | They called `git ls-files`, which raises outside a repository, so unzipping the archive and running `make test` produced two failures. The archive form — checking the files actually present — is the stronger of the two |
| 2026-08-31 | RISKS #43 opened rather than claiming live is hardened | It ran once, end to end, for one window. The guards are tested; a full ten-minute session, a mid-session network drop and concurrent sessions are not. Saying so costs nothing and claiming otherwise would be the kind of overreach this project has avoided throughout |
| 2026-08-31 | `evidence/raw-results/` is committed; the earlier decision to ignore it is reversed | It was called 'regenerable noise'. The picker lists a fixture only if a run document exists, and the stream reads cards from those files, so the archive offered zero channels and zero cards. Regenerable is not the same as present, and nothing tells a judge to regenerate |
| 2026-08-31 | The gate is run by opening the archive and using it, not by checking routes return 200 | Every route returned 200 while the page showed nothing. A dead product page fails silently; only using it the way a judge will exposes that |
| 2026-08-31 | P0 1.2 recorded as not-reproducing rather than fixed | Twelve abrupt mid-stream resets and six overlapping abandoned streams leave the server healthy. It was almost certainly cured by the ThreadingHTTPServer change made for another reason; inventing a second fix for a bug that no longer exists would be guessing, which the brief explicitly forbids |
| 2026-08-31 | The mid-stream disconnect property is pinned by test even though it passes | It was restored by accident. A property that nobody asserts is one refactor from regressing, and this one takes the whole demo down when it does |
| 2026-08-31 | Stale SSE events are dropped by epoch, including `done` | Two clicks in a second open two streams. Without the epoch the abandoned stream keeps appending, and its `done` disables the controls of the one the viewer is watching |
| 2026-08-31 | The chat feed yields to a citation rather than pinning to the bottom | A card cites the start of its window, ~100 rows above the live edge, so the highlight fired off-screen every time. The one gesture the product rests on was invisible in every session anyone has watched |
| 2026-08-31 | Scrolling up is treated as intent, not noise | Following stops and stays stopped until the reader clicks back. A feed that yanks itself to the bottom while someone is reading is the reason people distrust live views |
| 2026-08-31 | Embeddings enter as a measured arm or not at all | The team measured embedding clustering twice — Oct 2025 and Mar 2026 — and got ~100 unstable clusters both times. Re-adding it as the answer would be repeating a known negative; as a third arm with frozen labels it is either an improvement or a removed experiment with a result, and the changelog needs the second |
| 2026-08-31 | Grouping is a reducer bug fix, not tuning | `reduce.py`'s own docstring promises to collapse duplicates and retain counts; on a word-guessing stream it splits one signal across dozens of one-message groups and the board draws each as its own card. It touches no metric definition, no gold label and no gate code, and the frozen cases are scored on the agent's cards, not on these groups |
| 2026-08-31 | The grouping rules live beside `reduce_chat`, and `reduce_chat` is untouched | Its output is rendered into the agent's prompt and therefore hashed into every recorded model call. Rewriting it would miss the cache on all eleven frozen cases and take keyless replay down with it — the one property the whole submission rests on |
| 2026-08-31 | Group keys are namespaced by rule (`p:` / `t:`) | `jump` is reachable both as a 4-character prefix bucket and as a content token. Sharing one dict key let whichever rule ran second silently relabel the other's group |
| 2026-08-31 | A prefix bucket folds into the word it is a prefix of | Rule B runs first, so single-word `VIOLET` landed in `viol…` while the sentences landed in `violet` — measured 8 and 19 on marlon w6. One signal drawn as two rows is the exact bug being fixed, so they merge and the row reads `violet × 27` |
| 2026-08-31 | Ungrouped messages are reported, never shown as rows | A board showing six rows out of 1288 messages reads as the whole window. `grouped_summary` returns the count that was left out so the footer can say so |
| 2026-08-31 | The plan's `para… × 38` and `rang… × 21` are published as the measured 41 and 20 | The figures in `DASHBOARD.md` were estimated against a slightly different rule set. `violet × 27` reproduced exactly; the other two did not, and a number that was not measured by the shipped code does not go in a document |
| 2026-08-31 | Board attribution is two-tier — `matched` and `preceding` — and says which | Nearest-preceding alone attached 41 people brute-forcing an on-screen word puzzle to a caption reading *"three people sleeping in a dimly lit room"*. A row header reads as causal however it is captioned, so a board that cannot tell a lexical match from mere adjacency asserts something false |
| 2026-08-31 | Neither tier goes through the provenance gate | Neither claims causation — one is word overlap, the other is arithmetic on timestamps. The gate judges the agent's cards, and its ledger sits in the same rail counting those separately, so the two are never confused |
| 2026-08-31 | A group with nothing before it is UNATTRIBUTED, never given the nearest trigger anyway | Inventing a cause to avoid an empty header is the exact failure the gate exists to prevent, one layer up in the UI |
| 2026-08-31 | A merged row is as strong as its strongest group, not its first | Several groups can share one trigger. If any of them names the word the trigger contains, the moment is a real link and the row says so |
| 2026-08-31 | A wh-word counts in either of the first two tokens; an auxiliary only in the first | Anchoring at position zero lost "whats the game" and "yo what game"; allowing an auxiliary second turned "Capri is 19" and "There is a whole P star right there" into questions. A panel headed QUESTIONS TO YOU cannot afford statements in it |
| 2026-08-31 | The `54 junk hits` figure from FEATURES_V2 is published as the measured 29 | That number was quoted for "one marlon window" without saying which. On marlon 0715 w6 the bare `ends with ?` rule returns 29, of which at least 6 are literally `?`-only. The measured number replaces the quoted one |
| 2026-08-31 | The middle column shows the board or the signals, never both at once | A deterministic row and a gated card drawn in one column read as the same kind of claim. A segmented control makes the distinction physical, and the gate ledger stays in the rail so card counts are never hidden by the tab that is not open |
| 2026-08-31 | The board is computed on the server, not in the browser | The client never holds the fixture, and `/api/board` and the stream have to agree exactly on a window's numbers or the page and the endpoint would disagree about the same 60 seconds |
| 2026-08-31 | Clicking a board row highlights every message behind it | The same gesture the citation highlight already makes, driven by the group's own `event_ids`. It is what makes a count of 27 checkable rather than asserted |
| 2026-08-31 | The link strength is drawn with line weight, not colour | The palette carries no state colours and DESIGN.md forbids saturated ones; `matched` gets an ink hairline and `preceding` a grey one, so the hierarchy survives without inventing a token |
| 2026-08-31 | The rail is the first zone dropped below 1280px | It is the glanceable zone. The board is the one a streamer reads, and dropping the chat would leave the counts with nothing to check against |
| 2026-08-31 | Four dashboard guards were rewritten, not deleted, when the layout changed | They asserted two panels, a heading that moved into a tab, a tracking value and a function that was split. Every one still asserts the property it was written for, against the new structure |
| 2026-08-31 | An answer needs two shared content words, not one | Measured on marlon, a single-token rule answered *"whos fucking dad"* with *"the fucking water is this shit"* and *"WHYS SHE HERE"* with a line containing "here" — nine of its ten answers were that kind of coincidence |
| 2026-08-31 | The stricter rule loses *"ETA?"* → *"ETA 10:36."* and that is accepted | One shared token, genuinely correct, now marked unanswered. Telling a streamer they answered something they did not takes it off the list they came here for; leaving an answered question on the list only adds noise to it |
| 2026-08-31 | The panel shows the line the streamer actually said, not just a verdict | The answered link is lexical, the same family as a `matched` board row. A verdict with no evidence under it is what this project refuses everywhere else, so the reader judges it |
| 2026-08-31 | Questions are cumulative across the stream, not per window | "Asked four times" is a fact about the audience. Split across three 60-second tiles it becomes three quiet questions instead of one insistent one |
| 2026-08-31 | Questions is a third tab, not a fourth zone | The middle column already shows one kind of claim at a time and this is a third kind — stream-level rather than window-level. A fourth column would shrink the board, which is the zone a streamer actually reads |
| 2026-08-31 | The analysis window stays at 60 s; only the grouping refresh moves | Three cadences were being treated as one. Shrinking the analysis window changes the prompt bounds, so every cached response dies, replay becomes all misses, and the 11 frozen cases — verified today as carrying exactly 60000 ms spans — and their gold labels are invalidated. That is the whole comparison, a day before the deadline |
| 2026-08-31 | Live counts are computed on the server, not ported to JavaScript | Grouping is one set of rules with tests pinned to measured numbers. A second implementation in the browser would drift, and there is no JS runtime here to test it with. Measured at 0.4 ms per recount and 0.19 s to build a whole fixture's script |
| 2026-08-31 | A tick carries no trigger field at all | A live count drawn like a finished row would assert attribution it does not have. Rather than trusting the UI not to render a cause, the payload has none to render |
| 2026-08-31 | Tick groups are trimmed to 12 ids and 2 samples, and repeats are dropped | The full group dict was 966 KB per fixture — more than the chat it described. The complete set is on the attributed row at window close, where it is actually used |
| 2026-08-31 | The 30 s window measurement in WINDOWS.md is recorded as post-deadline work, not adopted | Its table is a measurement I did not take. The claim I verified myself is the one I act on: all 11 cases span 60000 ms |
| 2026-08-31 | Masonry only above 1500px, single column below | In the 600px board two tracks are 290px each — narrower than the quote line each row has to carry. Masonry there costs legibility and buys nothing, and the whole point of the layout is that a row with six groups and a row with one both look deliberate |
| 2026-08-31 | Rank still reads top-left first under masonry | CSS columns fill top-to-bottom, so the strongest row stays the first thing under the heading. That is the property that made masonry acceptable for a ranked list at all |
| 2026-08-31 | Rows space themselves with margins, not a flex `gap` | `gap` does not survive the switch to columns, so the same rows would collide the moment the viewport crossed the breakpoint |
| 2026-08-31 | The row entry animation reuses the signal cards' transition | One motion vocabulary rather than two. A second easing and duration for the same gesture is how a page starts feeling assembled instead of designed |
| 2026-08-31 | Reduced motion restores the resting state, not just the duration | Rows start at `opacity: 0`. The existing global block only zeroes the duration, so a row that never received its class would have been invisible rather than still |
| 2026-08-31 | FEATURES_V2 §4 was verified as already shipped rather than rebuilt | Unique chatters, new chatters, messages per chatter, top-10% concentration, the 10s sparkline, peak burst and velocity all landed in the rail in iteration 50 and were measured against the spec before writing any code |
| 2026-08-31 | The grounding fix ships as a second arm, `agent_grounded`, not as a re-recorded `agent` | FIX_GROUNDING_AND_UI §2 prescribes re-recording the agent. That changes every cache key, and the committed cache is how a judge reproduces every published number with no API key. A second arm measures the same thing and leaves the published comparison standing whichever way it goes |
| 2026-08-31 | Inlining is off by default and the eval does not run the arm without `--grounded` | An arm with no recording raises CacheMiss and exits 3 on a clean clone. `make eval` must keep reproducing from the committed cache, so the flag stays out of the Makefile until the arm is recorded |
| 2026-08-31 | The default opening turn is pinned by a test against a real committed cache entry | It is the single string the whole reproduction claim rests on. A one-character edit makes every entry a miss, and nothing else in the suite would have noticed |
| 2026-08-31 | The inlined list is named as the only source of trigger ids, with abstention kept explicit | Showing candidates without saying they are the candidates leaves the same ambiguity that produced the failure. `unknown` stays available, so this widens what the model can see without narrowing what it may answer |
| 2026-08-31 | Blank transcript segments are dropped from the inlined list | Deepgram emits them. An id over a blank line spends one of twelve slots telling the model nothing |
| 2026-08-31 | `make replay` and `make baseline` now default to a recorded fixture | They defaulted to the `sample` scaffold, which has no recording, so the bare command exited 3 with "nothing is recorded for this fixture yet" — while README.md line 172 promises those exact commands run with no keys. That reads as a broken submission rather than a wrong flag |
| 2026-08-30 | The grounded arm was measured and NOT adopted | Same recall (0.182), worse unsupported rate (0.882 vs 0.739), worse trigger accuracy (0.000 vs 0.500) on the same eleven cases. §2 says keep the measurement and revert; the arm was already off by default and behind `--grounded`, so nothing published moved and the code stays as the reproducible record of a negative result |
| 2026-08-30 | The negative result is committed with its cache rather than deleted | A lost experiment is only worth reporting if someone else can re-run it. 22 entries, 70 hits / 0 misses in replay with every key unset |
| 2026-08-30 | The grounded artifact goes to `evidence/grounded/`, not `evidence/` | `make eval` regenerates `evidence/` without `--grounded`, so writing the arm into the headline table would produce a report that the documented command cannot reproduce |
| 2026-08-30 | `DEEPSEEK_API_KEY` is dead; `OPENAI_API_KEY` is the working key | The provider reads `TS_LLM_API_KEY` then `DEEPSEEK_API_KEY`, and the latter returns 401 `invalid_api_key` against the configured base URL. Probed all four candidate names with a 1-token request; values never printed. Recording now needs `TS_LLM_API_KEY=$OPENAI_API_KEY` |
| 2026-08-30 | Diary dates are disclosed in RISKS #44 (renumbered from #36, which was taken), not mass-rewritten | 8 PROGRESS headings and 57 DECISIONS rows read `2026-08-31`; the clock says `2026-08-30 17:40 UTC`. No measurement depends on a timestamp — spend comes from token counts inside cache entries — and rewriting 65 rows with 24 hours left and no video shot is the wrong use of the time |
| 2026-08-30 | Cards show the cited messages, not a count of them | `Evidence — 3 messages` asks to be trusted; the whole product is that you do not have to. Two verbatim, the rest behind a disclosure, attached server-side because the browser never holds a fixture |
| 2026-08-30 | An unresolvable citation renders as unresolvable rather than being dropped | Dropping it would quietly show one fewer citation than the card claims. Shown, it is the provenance gate made visible |
| 2026-08-30 | The trigger timestamp rides beside the card, never inside it | The card is the recorded artifact and what is drawn has to stay diffable against what was scored |
| 2026-08-30 | The cause line shows `speech · 04:12`; the id moves to a tooltip and the debug panel | A raw uuid in front of a streamer is noise. A judge still needs the id, so it stays reachable rather than deleted |
| 2026-08-30 | Playback defaults to 4x and the control says it restarts | The first card lands when its 60s window closes, so at 1x a judge watches an empty column for a minute and reads the page as broken. Changing speed does restart the stream, and surprising someone is worse than telling them |
| 2026-08-30 | The no-grounding finding carries its census and updates live | It read as a bare claim. It now states how many were rejected and which code dominates — the numbers were already in the rail, so the claim and its evidence sit together |
| 2026-08-30 | `hero()` is kept, with a comment saying why | §4h says delete it if unused. It is used: `method.js` reads `payload.hero` and plays it in `stagePlay()`. Checked rather than assumed |
| 2026-08-30 | Tier 0 live chat is a separate module and a separate route from the paid live path | `/api/live` spends money per window; `/api/live_chat` cannot spend anything. A viewer must not reach the paid path by accident, and the free one must be obviously free by construction rather than by care |
| 2026-08-30 | `websockets` moved into the base requirements | It was grouped with `streamlink` when that was split out for needing Python 3.10+. Measured: `websockets` declares `>=3.9`, which is what macOS ships. Tier 0 is keyless, model-free and free, so it has to work on the base install a judge does |
| 2026-08-30 | The requirements guard now asserts the invariant instead of naming a package | It said "websockets not in core", which was incidental. It now says nothing in the base install may require Python 3.10, checked against installed metadata — a stronger test than the one it replaced |
| 2026-08-30 | Tier 0 states that it has no cause to give, in the status line | No transcript and no captions means no group can be attributed. Saying so is the same argument the chat-only ablation makes in the evaluation; letting an empty board imply a bug would be worse than the limitation |
| 2026-08-30 | Live viewers are pseudonymised even though nothing is stored | A demo gets filmed. A real viewer's login does not belong in a submission video, and the salt is per-session so no `.capture_salt` is created |
| 2026-08-30 | The mode control carries intent; the header badge carries the server's report | Two separate things, drawn separately, so a tab that thinks it is live over a replaying server cannot say so. The badge string is built only from what the server sent |
| 2026-08-30 | The agent graph is generated from the code, not drawn by hand | Hand-drawing would have been faster and wrong within a day. Every label, bound and count is read from `workflow/agent.py`, `provenance.py` and the committed trajectories, so a diagram that disagrees with the system is a failing test |
| 2026-08-30 | Published as an `<img>`, not inline SVG or a chart library | It draws with no network and with JavaScript off. The DESIGN.md palette is baked in because an image cannot read the page's stylesheet, and a test asserts every hex is one `app.css` already uses |
| 2026-08-30 | The diagram states 4 tools and 8 + 2 gate codes, against the plan's "five tools, eight codes" | Read from the code: `ALLOWED_TOOLS` has four, `check_card` has eight and `check_abstention` two. The plan's figures were stale, which is the exact drift a generated diagram exists to prevent |
| 2026-08-30 | Real call counts go on the tool edges | `get_frame_captions` is the smallest at 2 across 118 runs against `group_repeated` at 57. That is the grounding failure in one figure instead of a paragraph, and it makes the picture evidence rather than decoration |
| 2026-08-30 | `make graph` is a target, and a test asserts the committed file matches it | A generated artifact nobody regenerates is worse than a hand-drawn one, because it looks authoritative |
| 2026-08-30 | The shot list was rewritten against the built product, not patched | It quoted 447 passed against a real 555, described a two-column page that no longer exists, and said the grounding fix was "not applied" — which stopped being true when the arm was recorded. A shot list is filmed from once, under time pressure, and nobody re-checks it on the day |
| 2026-08-30 | Four guards now hold the shot list to the product | Test count, every figure in the published table, absence of the old interface's language, and that the grounded arm is called a failure. It went stale silently once; the same drift now fails `make test` |
| 2026-08-30 | Both test-count guards skip on a partial run instead of failing | They compare a documented number against the collected suite, so `pytest tests/test_docs.py` failed them for the wrong reason. A judge running one file is exactly the person who should not meet a red herring |
| 2026-08-30 | The video's strongest 40 seconds is the experiment that failed | Diagnosis from the cache, a fix applied as a second arm, a measured loss, and a refusal to adopt it. That sequence is worth more than any feature demo in the cut |
| 2026-08-30 | Renamed Twitch Agent → Twinky | Verified first that the product name appears in no prompt string — agent SYSTEM, CARD_CONTRACT, INTRO, TOOLS_DOC, the baseline prompt and the vision module are all clean — so the rename could not move a cache key. Entries above this row use the former name and are left as written; rewriting an append-only record retroactively is the history-tidying this project refuses everywhere else |
| 2026-08-30 | The distribution name `twitch-agent` in `pyproject.toml` was left alone | RENAME.md rules the Python package, module paths and CLI out of scope. `ts` is the import name and appears in every command in `REPRODUCTION.md`; renaming the distribution regenerates egg-info and risks `pip install -e .` with a day to go, for no gain a judge can see |
| 2026-08-30 | The app.css note keeping the old name was generalised, not rewritten | It records why the mark exists — a finding about the old two-word name. Renaming inside it would falsify the history, so the point was generalised and the original name kept as an explicit parenthetical |
| 2026-08-30 | The coordinated-repeats rule got a length floor, and that floor is the feature | Without it the rule flagged `ranger` from 15 accounts, `AURA` from 20 and `LOL` from 11 — normal Twitch, and `ranger` is the audience signal the board exists to surface. A moderation panel calling the product's own best output suspicious is a false-positive machine, and a false positive here is unrecoverable |
| 2026-08-30 | NEEDS A LOOK sits on the Method page, not the dashboard | The rule that earns its place is prompt injection, which is a security story before it is a moderation one. The dashboard is also what the video is filmed from, and RENAME.md rules layout changes out until that is shot |
| 2026-08-30 | Rules with zero hits are shown as rules with zero hits | Coordinated repeats and prompt injection both find nothing on these fixtures. Hiding them would leave a reader unable to tell "we checked and it is clean" from "we never checked", and inventing an example to fill the panel is the one thing this feature must not do |
| 2026-08-30 | The panel renders with `textContent` only, and a test enforces it | It draws the exact text that tried to attack the system. It is the one place in the UI where writing HTML would turn the report into the vulnerability |
| 2026-08-30 | The section-order guard now asserts consecutive numbering instead of a fixed list | It failed the moment a fifth section existed — punishing the page for growing rather than for breaking |
| 2026-08-30 | A shared `strip_js_comments` helper went into conftest | Sixth time a guard fired on the comment explaining why the forbidden thing is absent: `/* textContent, never innerHTML */` is not a use of innerHTML. Fixed once, in one place, instead of a seventh local patch |
| 2026-08-30 | Group labels are cosmetic and live in a separate `meaning` field | The token the reducer produced stays in `label`, untouched, beside it. Two fields rather than one, so the UI can always fall back and a diff between a labelled and an unlabelled run shows only additions. The gate and the scorer never read either — asserted by test |
| 2026-08-30 | Every failure path in the labeller returns `{}` | Cache miss, provider error, malformed JSON, or a label for a group that was never sent. Replay raises on a miss by design, and a demo that died because a cosmetic string was not recorded would be trading the product for decoration |
| 2026-08-30 | The first batching was wrong and was re-recorded rather than kept | Per-row batching cost 45 calls on an 11-window fixture; FEATURES_V2 §2 specifies one call per window and measured 10. The 45 orphaned entries were deleted so the committed cache holds nothing that will never be hit, and the $0.0017 they cost is still in the ledger because money spent is money spent |
| 2026-08-30 | `MAX_GROUPS_PER_CALL` raised from 8 to 16 after measuring | Windows hold 15-16 groups on the real fixtures, so at 8 the tail fell back to tokens for no reason. The call is already batched and the extra groups are a few dozen tokens each |
| 2026-08-30 | A label for a group that was not sent is dropped, not attached | It is a hallucinated key, and attaching it would put a caption over messages the model never saw |
| 2026-08-30 | The pair labels are committed in their own commit, containing no arm code | "Frozen before any arm ran" is worth nothing as a claim and everything as a fact. The freeze is a commit a judge can inspect: labels and checksum in, embedding code nowhere in the tree |
| 2026-08-30 | Singletons get unique ids rather than a shared `other` bucket | One `other` cluster would make every unrelated singleton a positive pair with every other, inflating recall for any arm that over-merges — which is precisely the failure being tested for |
| 2026-08-30 | Ambiguous messages are `unsure` and excluded from scoring, not forced into a cluster | Three per window. Forcing a call on "definitely" sitting inside a run of `dr…` guesses would punish or reward an arm for my uncertainty rather than for its own behaviour |
| 2026-08-30 | The labels are declared model-drafted and unreviewed, like `evals/gold` | I read the messages and assigned the intents; no person has confirmed them. Any number computed against them inherits that caveat and has to carry it |
| 2026-08-30 | Two windows of deliberately different shape | A word-guessing minute with two puzzles, and a varied minute of topics and replies. One window would let an arm look good by being right about a single shape |
| 2026-08-30 | Grouping arms are scored on pair precision AND recall, never on compression | An arm that merges every message into one group compresses perfectly, scores recall 1.000, and is useless. A test asserts precision catches exactly that, because a metric that cannot catch the degenerate case is not a metric |
| 2026-08-30 | A message an arm never grouped pairs with nothing | Treating two absent group ids as agreement would score silence as a correct answer. Both are groups of one, and two groups of one are not a pair |
| 2026-08-30 | The measured A/B result is published with the `reviewed: false` caveat attached to every figure | The labels are model-drafted. They separate two arms by a factor of four, which they are good enough for; they are not good enough to call a 2-point difference, and the README says so |
| 2026-08-30 | Arm C's full threshold sweep is published, not its best point | C has a free parameter A and B do not. Quoting one row would be quoting a number chosen by looking at the answers, and a test now fails if the sweep leaves the write-up |
| 2026-08-30 | Embeddings measured and NOT adopted — and not because they lost | Best pooled F1 0.583 against B's 0.403, but that is an averaging artifact: the same threshold scores 0.770 on one window and 0.164 precision on the other. The threshold does not transfer, which is the instability the team recorded twice |
| 2026-08-30 | An embedding replay miss raises rather than scoring zero | Unlike a cosmetic label, a missing embedding means the arm cannot be scored at all. Returning nothing would publish a zero that reads like a measurement |
| 2026-08-30 | The shipped grouping stays free and keyless | The whole grouping path runs in Tier 0 live chat with no provider. Arm C's ~3-point recall gain at matched precision does not buy back an API key, a network round trip per window, and a threshold that cannot be chosen honestly in advance |
| 2026-08-30 | `make scan` no longer fails for a git-ignored `.env` | It could never pass on the author's machine, and a security check that always fails is one you learn to ignore. `make archive` builds from `git archive HEAD`, so an ignored file cannot enter the zip by construction — the scanner now checks `git check-ignore` and says so out loud rather than staying silent |
| 2026-08-30 | Outside a git checkout nothing is excused | The extracted archive has no git, so `is_git_ignored` returns False there and a local-only file being present is a real failure — which is exactly the case the check exists for |
| 2026-08-30 | The clean-clone verification is re-run whenever the tree has moved a lot | It has caught a gitignored evidence directory, `git ls-files` failing in the archive, and `make setup` dying on Python 3.9. It is the pre-scoring gate: a submission that cannot be run is never scored |
| 2026-08-30 | README and SUBMISSION were audited against the built product, not trusted | Both described a two-column page and 33 trajectories for roughly fifteen iterations after each stopped being true, and mentioned none of the board, rail, Tier 0, agent graph, moderation panel, group labels or the three rolled-back experiments. They are the first documents a judge reads |
| 2026-08-30 | The three rolled-back experiments were promoted into SUBMISSION.md | Building something, measuring it and declining it is the part of engineering that usually leaves no trace. It was buried in the changelog while the document a judge opens first said nothing about it |
| 2026-08-30 | Four guards now hold the top-level documents to the product | Trajectory count against the real file count, the interface description, the presence of the rolled-back experiments, and the grouping figures checked against what the scorer prints today. The shot list rotted silently once; these fail `make test` instead |
| 2026-08-30 | The architecture diagram gained a second, wholly deterministic pipeline | The reporting layer is not one node called "live rail". It is grouping, board, rail, questions, moderation, labels and the generated graph — and it calls no model except one cosmetic label call, which is what makes the whole of Tier 0 possible with no key |
| 2026-08-30 | `docs/REPRODUCTION.md` now documents the two separately-reproduced results | `SUBMISSION.md` tells a judge to run `--grounded` and `score_arms`; the file whose entire job is reproduction did not mention either. Both verified end to end before being written down |
| 2026-08-30 | The guide's description of the secret scanner was corrected with its behaviour | It said a local `.env` is fatal. It is now reported and allowed when git confirms it is ignored, and a guide that contradicts the tool teaches the reader to distrust the tool |
| 2026-08-30 | The coding-agent disclosure's false claim was corrected in place, with a note saying so | It stated the product-agent trajectory directory was "empty today because no run has been recorded yet". True when written, false for most of the project's life, and sitting in the one document whose entire purpose is accurate attribution. A disclosure that silently repairs its own errors is not one |
| 2026-08-30 | The disclosure's findings list was extended with what the later sessions found | It stopped at the early defects and claimed paid calls were zero. Ten paid calls totalling $0.43 have happened, and the biggest findings — an invalid first evaluation, silently broken keyless reproduction, the grounding diagnosis — came after it was written |
| 2026-08-30 | RISKS #39 now says diagnosed, fixed as an arm, measured and not adopted | It read as an unexamined open failure. The shipped agent still has it, which is why it stays open — but the record should show it was quantified and acted on, not merely noticed |
| 2026-08-30 | The markup-to-script wiring is now asserted, one iteration before filming | A single `getElementById` returning null throws on the next property access and kills the page mid-stream. Nothing else in the suite caught that class: the server tests assert payloads, the CSS tests assert tokens, and neither touches whether the code and the markup still agree after twenty iterations of editing both |
| 2026-08-30 | Markup balance is parsed, not eyeballed | A stray unclosed `<section>` silently swallows everything after it. One was introduced while moving the moderation panel below the results and only a parser found it |
| 2026-08-30 | The dynamic id is allowed by name, with its guard asserted | `signals-finding` is created by the code and read behind `if (!note)`. Whitelisting it without also asserting the guard would have made the test a rubber stamp |
| 2026-08-30 | Gold-label confirmation is a checked command, not a hand-edit across eleven JSON files | `REVIEW_ME.md` promised a ten-minute review needing no JSON, then asked for exactly that. A stray comma at three in the morning is a broken eval, and the flag is too important to risk on manual editing |
| 2026-08-30 | There is deliberately no `--all` | Eleven labels behind one keystroke is how a review becomes a rubber stamp. The flag exists to separate a review that happened from one that was asserted, and a tool that makes asserting easy destroys the distinction it records |
| 2026-08-30 | Disagreement is recorded as `"disagreed"`, not left as `false` | A label a reviewer rejected is information. Left false it looks identical to a label nobody read |
| 2026-08-30 | An anonymous confirmation is refused | Whoever confirms is part of what makes it a confirmation. `--by` is required, and the change has to be committed, because the flag is only meaningful when the diff is visible |
| 2026-08-30 | A test asserts the committed labels are still unconfirmed | If it fails, either a person genuinely reviewed them — in which case the documents saying otherwise must change — or something confirmed them automatically, which is worse |
| 2026-08-30 | The duplicate risk numbers I created were renumbered to 44 and 45, never the originals | `RISKS.md` states that numbers are stable because other documents cite them. I added rows on two separate days reusing 36 and 37, which made the critical-path summary ambiguous about which #36 it meant — in the list the author works from in the final hours. The originals keep their numbers; my later rows moved |
| 2026-08-30 | Rows #2, #12 and #20 were restored as closed rather than their citations deleted | They were dropped from the table when they closed, while the critical-path summary and `PROGRESS.md` still cite them. A reference that resolves to nothing is worse than no reference |
| 2026-08-30 | `PROGRESS.md`'s reference to the old #36 is left as written | It is append-only. The renumbering is explained in the `RISKS.md` header instead, so a reader following the citation lands somewhere that tells them what happened |
| 2026-08-30 | `.env.example` must be safe to copy, and the model names are commented out to make it so | It set `TS_TEXT_MODEL=deepseek-v4-flash` against recordings made with `gpt-4.1-nano`. The model name is part of the cache key, so copying the file broke `make eval` outright. The code already defaults to the recorded models when the variables are unset, so the safe example is the one that sets nothing |
| 2026-08-30 | `TS_LLM_BASE_URL` is documented with the reason, not just the value | The built-in default is DeepSeek's endpoint. An OpenAI key sent there returns 401 `invalid_api_key`, which is exactly what happened during a record phase this session and cost an iteration to diagnose |
| 2026-08-30 | `TS_ESCALATION_MODEL` was removed from the example | Nothing reads it. A documented variable that no code consults reads as a feature that exists |
| 2026-08-30 | RISKS #37 was wrong in a way that would have published the wrong project | It said the remaining repository action was "make it public". `origin/main` is 31 commits behind local, so making it public without pushing would show a project called Twitch Agent with none of the grouping work, the live tier, the agent graph or the three measured experiments — and it would look finished, which is worse than private because nobody would check |
| 2026-08-30 | `make preflight` reports and never repairs | A checklist that fixes things is one you stop reading. It runs no push, no commit and no confirmation, asserted by test |
| 2026-08-30 | A network check that cannot run reports UNKNOWN, not PASS | An offline check that silently passes is worse than one that admits it does not know — especially the one guarding "is the public repository actually this work" |
| 2026-08-30 | Preflight separates hard blockers from stated costs | Unconfirmed gold labels are a cost the README already discloses, not a reason to stop. A checklist that cannot tell those apart gets ignored at three in the morning, which is when it will be read |
| 2026-08-30 | Three incomplete captures found on disk were left untouched, not deleted or committed | `hasanabi`, `yugi`, `zackrawrr` at `2026-08-30T2035`: `meta.json` and a gitignored `raw/` only, no derived events, 52 MB. Not created by these sessions. Committing incomplete capture data is wrong; deleting someone else's work unasked is not this session's call. Recorded as RISKS #48 and reported by preflight as untracked rather than as a blocker |
| 2026-08-30 | Preflight distinguishes uncommitted source from untracked captures | Blocking a submission on capture data that is correctly not committed is a false alarm, and a checklist that cries wolf once stops being read at the hour it will actually be read |
| 2026-08-30 | The secret scan gained a `--history` mode before the repository is made public | Publishing exposes every version of every file. `make scan` had only ever looked at current files, and a key committed once and removed in the next commit is still in the pack — invisible to a filename check because the leak is in the content |
| 2026-08-30 | The history scan was verified by planting a secret, not by trusting a clean result | A scanner that reports "clean" is worthless until it has been shown to report "dirty". Planted a key in a throwaway repo, removed it in the next commit, and confirmed the removed blob was found at its own sha |
| 2026-08-30 | It uses `git cat-file --batch`, one process for the whole history | One subprocess per object made it slow enough to be skipped, and a check that is slow enough to skip is one that gets skipped at the hour it matters. 0.6 s for 2110 objects |
| 2026-08-31 | Seven uppercase micro-labels moved from 11px to the DESIGN.md badge step | 11px is not on the scale at all. DESIGN.md defines uppercase badges as 12px / 600 / 1.40 / +0.96px, and six of the seven drifted there during the dashboard work — the most repeated element on the page was the least consistent |
| 2026-08-31 | The uppercase guard now checks size and weight, not tracking alone | It asserted `.96px` only, which is exactly why seven labels drifted to an undefined size without failing anything. A guard that checks one property of a spec licenses drift in the others |
| 2026-08-31 | 11px body text and the 13px tier are recorded as open questions, not changed in the same iteration | Nineteen selectors use 13px and five use 11px non-uppercase. Neither size is in DESIGN.md, but moving them is a real density change in bounded panels and deserves its own iteration with the layout checked, not a sweep at midnight |
| 2026-08-31 | Stated hours-to-deadline had drifted and is now read from the clock | I had been decrementing an estimate per iteration rather than checking: I reported ~13.5 hours when 21.0 remained. Corrected, and the figure is computed rather than carried forward |
| 2026-08-31 | 13px was documented in DESIGN.md rather than removed from the code | Nineteen selectors use it, including the group label on a board row, which is already ellipsised in an 8rem column. Forcing them to 14px widens text ~8% and loses characters off the product's most important element to satisfy a table. It was in use before it was written down, which is the wrong order — fixed by writing it down with the reason, not by pretending the table was already right |
| 2026-08-31 | The five stray 11px selectors moved to 13px, the nearest documented step | After that, no top-level `font-size` in `app.css` is off-scale |
| 2026-08-31 | Responsive step-downs are exempt, and DESIGN.md says so | 40px, 28px and 26px appear only inside `@media`. A responsive scale needs intermediate values; a test that exempts a case must be able to point at the sentence allowing it, so it asserts that sentence exists |
| 2026-08-31 | Type sizes are now pinned to DESIGN.md the way colours always were | Colours have had that guard since early on; type never did, so the scale drifted on both sides at once and nothing failed. Verified in both directions by planting a 17px rule — rejected at top level, allowed inside `@media` |
| 2026-08-31 | The board row's samples wrap to two clamped lines instead of one ellipsised line | They are the row's evidence — the claim is "do not trust the count, here are the messages" — and measured across the fixtures, 30 of 276 group sample-lines (11%) were being cut off, the longest at 334 characters. Two lines recover almost all of it; the median line is 34 characters and never wraps at all |
| 2026-08-31 | Clamped rather than free-wrapping | A 334-character sample line would otherwise let one group swallow the board. Clipping that outlier is correct; clipping the median was not |
| 2026-08-31 | The proportional bar's fill went one step darker, still with no colour | `--muted-soft` on a `--hairline-soft` track is barely a difference, and the bar exists for exactly one job: telling 27 from 4 without reading |
| 2026-08-31 | The four rich click targets became operable regions, not buttons | A `<button>` may contain only phrasing content, and a board row is a header, a quote, group lines and samples. `role="button"` with `tabindex="0"` and Enter/Space is the correct pattern for a composite region |
| 2026-08-31 | Every operable region carries an `aria-label` naming what activating it does | "Highlight the 27 messages behind this row", not "row". A focusable thing that does not say what it does is a tab stop, not a control |
| 2026-08-31 | Space calls `preventDefault()` | Otherwise it scrolls the page while also activating the row, which is the classic half-implemented version of this pattern |
| 2026-08-31 | No new focus CSS was needed | `:focus-visible` already draws the ink ring on anything focusable, so making the regions focusable was the entire fix. The guard now also asserts `outline: none` never appears |
| 2026-08-31 | Empty states say what will happen, not that nothing has | The board read "Waiting for the first window to close…" — copy written before live counts existed. Measured: counts appear at 2.0s and rows at 60.0s, so it told the reader to wait a minute for something already under way, on the first thing a judge sees |
| 2026-08-31 | The signals empty state was rewritten too, though its wording was accurate | It was the last passive one on the page. Accurate and uninformative is still a wasted first impression, and it now says what the gate does with the rest |
| 2026-08-31 | The markup and the reset path must carry identical empty copy, asserted by test | A reset that renders different words than a page load is two products in one page. I only avoided that drift this time by checking both by hand |
| 2026-08-31 | The citation scroll now reads `prefers-reduced-motion` in JavaScript | CSS cannot reach it: `scrollIntoView({behavior: "smooth"})` passes the behaviour explicitly and overrides the stylesheet's `scroll-behavior: auto`. A reader who asked for less motion still got a smooth scroll on every citation |
| 2026-08-31 | Reduced motion renders the hero stage instead of skipping it | It means "do not animate", not "do not show me the content". `renderStage` skipped `stagePlay` entirely, so the Method page's only real-data demonstration rendered as an unhidden, completely empty box — the accessibility path was strictly worse than no accessibility path |
| 2026-08-31 | `stagePlay` gained an `after()` helper rather than four conditionals | Every beat runs through one place, so a new beat cannot quietly be animation-only. A test asserts all four go through it |
| 2026-08-31 | The preference is read at use, not cached at load | It can change while the page is open, and this is the gesture the product rests on |
| 2026-08-31 | The product rail was renamed `#window-rail` rather than scoping the CSS harder | Two different components under one id is the defect; a more specific selector would have hidden it. The Method page keeps `#rail` because its card rail is what that rule was written for |
| 2026-08-31 | Shared ids are allowed but enumerated | `debug`, `picker`, `mode-badge` and friends are genuinely the same component on both pages. The guard lists them and fails on anything new, because the next unlisted collision is another `#rail` |
| 2026-08-31 | A test now fails on any styled id that no page renders | `#rejected-rail` and `#abstained-rail` were styled for weeks and existed nowhere. Dead CSS reads as a feature to whoever edits next |
