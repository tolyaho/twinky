# Risks to the qualification gate

Reviewed 2026-08-30 03:50 MSK. Numbers are stable across revisions — `PROGRESS.md` refers to
them — so rows are reordered by severity, never renumbered.

Two rows added on 2026-08-30 reused numbers that were already taken (36 and 37), which made the
critical-path summary below ambiguous about which #36 it meant. The **originals keep their
numbers** because other documents cite them; the later additions became **44** and **45**. A test
now fails if any number is used twice.

## The critical path, and what it costs

Reviewed against the tree 2026-08-30 14:30, by executing claims rather than re-reading them.
**#2 is closed** — four fixtures captured, eleven cases frozen, one measured comparison, all
reproducible from cache with no keys. Everything that was downstream of it (#12, #20, #21, and
the whole B phase) is closed with it.

What is left is not engineering. In order: the **video** (#6) does not exist and is a hard
deliverable; the repository must be **pushed and then made public** (#37, #47) — it is 31 commits
behind local, so publishing it as it stands would show a project called Twitch Agent with none of
the grouping work, the live tier, the agent graph or the three measured experiments; the **gold
labels** (#36) are model-drafted and unconfirmed; and `.env` (#17) and the Telegram history (#1)
hold live credentials that need rotating regardless of the hackathon.

**`make preflight` checks all of this in one command** and exits non-zero while any hard blocker
stands. Run it before submitting rather than trusting this paragraph.

The measured result is mixed and is reported that way: the agent doubles the baseline's recall
and loses the headline unsupported-card rate. That is #38, a finding rather than a defect.

Decision gate is Sunday 20:00 MSK; submission is Monday 21:00 MSK (18:00 UTC).

---

## P0 — do these first

| # | Risk | Status | What to do |
|---|---|---|---|
| 17 | `.env` holds eight live credentials | **OPEN — rotation is the remaining action** | Twitch, Deepgram, Anthropic, OpenAI, DeepSeek and DB credentials. Packaging is now closed off three ways: `.env` is untracked and gitignored, `make archive` builds from `git archive HEAD` so an untracked file cannot enter the zip by construction, `.gitattributes` marks it `export-ignore` anyway, and a test asserts it is not tracked. `make scan` still reports it as a local-only shipping blocker. What is left is not packaging — the credentials are live and need rotating regardless of the hackathon. |
| 1 | Credentials pasted in the team Telegram group (Twitch secret, MySQL creds, Anthropic key) | **OPEN** | Rotate regardless of the hackathon. Never commit the export. Still live — see #16, #17. |
| 37 | The GitHub repository is private, so judges cannot access it — **and is 70 commits behind local** (re-measured 2026-08-31; it was 31 when this row was written) | **OPEN — author action, two steps not one** | `github.com/tolyaho/twinky`. `make scan` is clean and `legacy/README.original.md` was a false positive (#16), so there is nothing to scrub first. Flip it public before submitting, and check `legacy/frontend/` (#13) is acceptable to publish. |
| 36 | Gold labels are model-drafted and no human has confirmed them | **OPEN — author action** | All eleven gold files carry `"reviewed": false`. `evals/REVIEW_ME.md` is a ten-minute pass written for exactly this. README §6 states the position plainly rather than claiming a review that did not happen. Three cases need judgement specifically: c03's trigger, c12's abstain call, c05's warning inside an unrelated flood. |
| 6 | Video not started; ≤5:00 and a hard deliverable | **OPEN — the only missing deliverable** | Footage now exists: `make demo` runs from cache on two fixtures. Storyboard in `../notes/06-VIDEO.md`. A judge who cannot watch the demo cannot score End-to-End Quality at all. |
| 21 | The fixture recorder had never been run against a live stream | **CLOSED 2026-08-30** | Run four times against live broadcasts: 2 + 10 + 12 + 12 minutes, 3863 chat messages, 72 frames, 47.6 minutes of audio, all four enriched and committed. The two defects fixed beforehand were real — a re-run re-stamped already-timestamped frames from zero, and an empty capture returned success while writing a `meta.json` declaring a good fixture — and neither recurred. |
| 22 | `.env` is never read — nothing calls `load_dotenv` | **OPEN — new 2026-08-30** | Verified: zero call sites in `src/`, `evals/`, `tests/`, `scripts/`, `Makefile`. A correctly filled `.env` will still fail the record phase with "DEEPGRAM_API_KEY is unset". Export the variables in the shell instead. Note `.env` also sets `TS_LLM_MODE`, so auto-loading it would let a local file silently flip a judge's replay run into live mode — the fix is to export keys, not to add `load_dotenv()` blindly. `docs/REPRODUCTION.md` §10 was corrected. **Corrected 2026-08-31:** this row said "`python-dotenv` is declared", which stopped being true when #11 removed the six unused packages — it now survives only as a comment in `requirements.txt` explaining its own removal. The substance is unchanged: nothing reads `.env`, deliberately. |

## Open — required deliverables and scoring

| # | Risk | Status | What to do |
|---|---|---|---|
| 38 | The agent loses the headline metric to both other systems | **OPEN — reported, not fixed** | Measured: unsupported-card rate 0.739 against the baseline's 0.619 and the chat-only ablation's 0.280. The agent also doubles the baseline's recall (0.182 vs 0.091) and is the only system to name a correct cause. Root cause diagnosed and then narrowed 2026-08-30: 14 of 23 cards set `trigger.event_id` to a chat UUID, so 8 fail `E_CIRCULAR_EVIDENCE`. The obvious hypothesis — that the shared contract never states a trigger must be a speech or frame event — was checked and is **false**: the rule is in the contract, and 129 of 173 recorded requests carry it (the 44 without are the discarded first run). All eight failures name a chat id that is also in their own evidence, which two clauses forbid explicitly, and three of them set `kind: "unknown"` beside a concrete id. So this is `gpt-4.1-nano` not following a stated rule, not an under-specified spec. Not tuned after the fact — see `docs/IMPROVEMENT_CHANGELOG.md`. Fix in the next build and measure it on a fresh run. |
| 39 | The agent abstains without checking the screen | **OPEN in the shipped system — diagnosed, fixed as an arm, measured, not adopted** | On `c01_word_puzzle_amethyst` it called `group_repeated` and `get_transcript_window`, found no speech, and returned `none` reading "no clear speech or on-screen content detected" — never calling `get_frame_captions`, on the one case where the screen is the only possible cause. Visible step by step in the trajectory. Deliberately not fixed: forcing a frame check after seeing the score is tuning. **Quantified since:** across 70 recorded conversations chat results appear in 70 and frame captions in 2, and all 57 of the agent's own opening turns contain zero event ids — so it had no non-chat id it could name. Supplying the candidates (Removed experiment #2) took frame citations 0 → 4 and abstentions 5 → 0, but unsupported rate 0.739 → 0.882, so it was not adopted. The shipped agent still has this failure and the UI states it in place. |
| 8 | Only the latest *complete* submission is evaluated | **OPEN** | Submit a complete draft early Monday, then revise. |
| 7 | Submission ownership under the Participation Agreement, and there is no `LICENSE` file | **OPEN — author decision** | Read the actual agreement before uploading. Noted 2026-08-31: the repository has no licence of any kind, so publishing it leaves default all-rights-reserved copyright in force. That may be exactly what the agreement requires or exactly what it forbids — it is a legal choice with consequences, not a tidy-up, so no licence was added unilaterally. Decide it against the agreement before making the repository public. |
| 13 | `legacy/frontend/` shipped fabricated data inside the submission | **Fixed 2026-08-30** | It fabricates names, emotes and cluster values. A judge who opens it sees generated data inside the submission. Exclude or delete before packaging. |

## Open — accuracy and hygiene

| # | Risk | Status | What to do |
|---|---|---|---|
| 14 | Twitch VOD retention figures (7 / 14 / 60 days by tier) come from planning notes, not a source | **OPEN — UNVERIFIED** | Carries the product's second thesis, so source it before it enters the README or the video. Currently stated qualitatively, with no numbers. |
| 15 | Jan/Mar 2026 per-message cost and tail-latency figures are team recollection | **OPEN — UNVERIFIED** | The README states the failure qualitatively and quotes no figure. A number for the video has to be re-measured, not recalled. Supersedes the old #9. |
| 11 | `requirements.txt` declared six packages nothing imports | **Fixed 2026-08-30** | Removed `deepgram-sdk`, `fastapi`, `uvicorn`, `python-dotenv`, `orjson`, `pydantic`, and `pytest-asyncio` (zero async tests). Proved rather than asserted: a clean venv built from the reduced file plus `-e .` runs the full suite green. The replay path is now one runtime package, `httpx`. A test fails the build if a declared package is never imported. |
| 35 | `make scan` reported a placeholder docs block as the project's highest-severity secret | **Fixed 2026-08-30** | The scanner now recognises the placeholder forms (`your_*`, `changeme`, `replace-me`, `localhost`, `<...>`, `xxx`) and its own decoy-bearing test file is allowlisted. `make scan` reports zero project-file findings for the first time; only `.env` remains, correctly classified as local-only. Two holes found while fixing: `your\w*` greedily matched `yourself_is_not_a_placeholder...`, so a real secret beginning "your" would have been skipped (now bounded), and the comment-stripping ran before the placeholder check, so a line *starting* with `#` became empty and the comment explaining this very rule was reported as a leaked credential. 11 tests, one per form plus both holes. |
| 43 | Live capture is exercised once, not hardened | **OPEN — demo path, low** | One 60-second window against a live broadcast: captured, enriched, analysed, 104.8 s, ~$0.006. Guards are tested deterministically — refuses past a $3.00 cap, stops after ten minutes, sanitises the channel, treats offline as a message. What is NOT exercised: a full ten-minute session, a mid-session network drop, or two viewers starting one at once. It is a demo reached by explicit click; replay remains the default and the graded path, so a failure here cannot affect reproduction. |
| 5 | Vision model choice unverified | **Closed 2026-08-30** | `gpt-4.1-mini` has now produced 76 frame captions across four fixtures, all cached and committed. The default in code is the model the cache was recorded with, so a clone reproduces without configuration. The prompt was tightened after inspection to forbid describing the chat overlay — a caption that transcribes chat could otherwise become the claimed cause of the very messages it contains. |

## Mitigated or fixed

| # | Risk | Status | Evidence |
|---|---|---|---|
| 2 | No real fixture existed, so nothing downstream could be measured | **Closed 2026-08-30** | Four fixtures captured from live broadcasts, eleven cases frozen, one measured comparison, all reproducible from the committed cache with no keys. Row restored 2026-08-30 because the critical-path summary and `PROGRESS.md` both cite it and it had been dropped when it closed — a citation that resolves to nothing is worse than no citation. |
| 12 | No measured comparison between the agent and a fair baseline | **Closed 2026-08-30** | `evidence/report.md`, eleven cases, three systems, reproduced by `make eval` at 48 hits / 0 misses. Row restored with #2. |
| 20 | No evidence artifacts a judge could open | **Closed 2026-08-30** | `evidence/` holds the report, the per-case CSV, raw predictions with gate decisions, and 118 trajectories. Row restored with #2. |
| 3 | Judges cannot run live Twitch capture | **Mitigated, and now proved by execution** | Replay plus the committed model-call cache. 2026-08-30: C2's command list — `replay`, `baseline`, `eval` — is executed in the suite with every credential deleted from the environment and `socket.socket`, `create_connection` and `getaddrinfo` all rigged to raise. The chain completes on cache hits alone, and an unrecorded system exits 3 rather than reaching for a provider. A separate out-of-process test asserts `httpx` never even enters the module table of a replay run. The full C2 on recorded fixtures still waits on #2. |
| 4 | Frontend fabricates all data | **Mitigated for the shipped surface** | The dashboard renders only what `/api/replay` serves; a test fails the build on `Math.random` or an `innerHTML` assignment, and another requires every colour to exist in `DESIGN.md`. `legacy/frontend/` was removed from the tree on 2026-08-30 (#13), so no fabricating surface ships at all. |
| 10 | `python -m ts.cli` failed from a fresh clone | **Fixed 2026-08-30** | `pyproject.toml` declares the src layout; `make setup` runs `pip install -e .`. Verified with no `PYTHONPATH`. **Caveat:** the checked-out `.venv` was built by `uv` and has no `pip`, so the editable install was confirmed through `uv pip`; the literal `python3 -m venv` + `pip` path is only proven by the C2 fresh-clone run. |
| 18 | `make scan` gave false assurance for the whole project | **Fixed 2026-08-30** | Three defects at once: `grep -r .` on macOS never reached `.env`; the pattern list lived in the Makefile so it matched itself and was permanently red; `legacy/` was excluded while remaining in the tree. Replaced by `scripts/scan_secrets.py`, 10 tests including a regression for each. |
| 19 | `trajectories/product-agent/` held 55 test artifacts and no real trace | **Fixed 2026-08-30** | Removed; `TS_TRACE_DIR` redirects the suite so the class of problem cannot recur. A test rejects any trace whose case id is not a frozen case or a tiled window. |
| 9 | Pricing/latency figures from unsourced planning docs | Superseded | Split into the specific claims #14 and #15 rather than a general policy line. |
| 16 | `legacy/README.original.md` lines 18–23 hold live credentials | **Withdrawn 2026-08-30 — scanner false positive** | Read the file instead of trusting the scan: the lines are `DB_PASSWORD=your_password`, `DEEPGRAM_API_KEY=your_key`, `TWITCH_OAUTH=your_token`, inside a "Create `.env`:" instruction block. Placeholders, not credentials, so there is nothing to rotate and `legacy/` stays preserved as-is per `docs/PRE_EXISTING.md`. The scan is what needs fixing: `scan_secrets.py` recognises `<your key here>` and empty values as placeholders but not the `your_*` form, so it reports a six-line docs example as the highest-severity finding in the project. Left open as #35 — a scanner that cries wolf on its own README gets ignored, which is the failure mode #18 already cost this project once. |

## Environment

| # | Risk | Status | What to do |
|---|---|---|---|
| 23 | `make setup` fails on this machine — `python3 -m venv` dies at `ensurepip` | **OPEN — machine-local, does not block a judge** | Reproduced 2026-08-30 on Homebrew 3.11 and 3.14; upstream of anything this project controls. `docs/REPRODUCTION.md` §2 documents the verified `uv venv` fallback. Running the gate as an actual fresh clone found the defects that *did* block a judge and they are fixed (see #40): the whole chain — setup deps, test, eval, inspect, baseline, replay, debrief — now runs from `/tmp` with every credential unset and zero API calls. |
| 40 | `make setup` failed from a clean clone on macOS system Python | **Fixed 2026-08-30** | `streamlink==8.0.0` requires Python 3.10+ while macOS ships 3.9 as `python3`, so pip died with a resolver dump at the first documented command — over a package no graded command imports. Capture-only dependencies moved to `requirements-record.txt`; `make setup` installs the graded path only and checks the interpreter first, failing in a second with an actionable message. Found by executing the gate rather than reading it. |
| 41 | Malformed model output crashed a run rather than being recorded | **Fixed 2026-08-30** | Two instances on real output: `{"cards": ["text"]}` hit `str.get` in `cap_cards` and killed a record mid-run *after* earlier windows had been paid for, and `"distribution": "single mention"` crashed the debrief on `.values()`. Model output is untrusted in shape, not only in content. Both guarded, with tests. |
| 24 | Two documents claimed a summary hierarchy that no module implements | **Fixed 2026-08-30** | `docs/ARCHITECTURE.md` listed it under "implemented nodes" and the README component table listed it unmarked among components that exist. Zero matches in `src/`. Both now mark it as a named, unbuilt gap; a test asserts the declaration stays in step with the tree. The README row was written on 2026-08-30 by carrying a design table over from `../notes/01-PRODUCT.md` without checking it against the code. |
| 42 | The one outward action — approve → draft poll — cannot be demonstrated | **OPEN — known gap, supporting figures corrected 2026-08-31** | `report/poll.py` is implemented, unit-tested and wired into the dashboard, and no draft is ever attached. **Both figures previously given here were wrong.** It said no card in any recorded run carries a `distribution`: across the three stream fixtures 37 of 68 do, all free-text strings like `"14 mentions"`. It said three verified `audience_answer` cards all have `distribution: None`: the eval has **nine** verified `audience_answer` cards, of which 8 are `None` and 1 is a string. Exactly one card anywhere carries a real `dict` — `c09_two_topics`, `{'is that my corn': 1}` — and it is **rejected** by the gate and degenerate anyway, one option with a count of one, so `build_draft` correctly returns `None`. **The conclusion is unchanged and now rests on the right numbers.** Same root cause as the debrief crash on a string `distribution`: the model answers with prose where the schema wants a mapping. Requiring a distribution in the prompt would likely fix it and would also be tuning after seeing results, so it is not being done mid-flight. Consequence: the last link of the invariant, evidence → streamer action, is code-complete but unexercised, and `video/SHOTLIST.md` says to cut that beat rather than stage it. |
| 25 | Twitch poll limits in `report/poll.py` (5 options, 60/25 chars) are recalled, not sourced | **OPEN — UNVERIFIED, low** | Nothing posts, so a wrong cap costs nothing today, and every trim is reported in the draft's `warnings` rather than applied silently. Check them against Twitch's API documentation before any real posting integration. |
| 26 | The provenance gate rejected every correct abstention | **Fixed 2026-08-30** | A `none` card claims nothing, so it cites nothing, so it failed on `E_NO_EVIDENCE`. Measured before the fix: a correct abstention scored `unsupported_rate = 1.0` — the headline metric — and landed in the dashboard's rejected block, so the demo would have shown the product's best moment as a failure. It also depressed cases 5, 11 and 12, the three the product is designed to win on. `none` now takes an abstention path that fails only on self-contradiction (citing messages or naming a cause), and a passing one is labelled `abstained`. 10 tests, including one that keeps the path from becoming a hole in the gate. |
| 27 | The provenance gate used an inclusive window end while every query path is half-open | **Fixed 2026-08-30** | `events.window` is `[start, end)` and the agent's tools follow it, but `check_card` accepted `<= end`. Demonstrated on the sample fixture: a message at exactly the tile boundary is invisible to window 1's tools, belongs to window 2, and the gate still verified a window-1 card citing it. Since tiles are adjacent, the gate was blind at every boundary to exactly the claim it exists to catch — understating the unsupported-card rate, the headline metric. Now half-open. Verified that all four frozen gold cards still pass, and a test keeps that true. |
| 28 | A one-word trigger quote satisfies the gate's verbatim check without proving causation | **OPEN — known limitation, deliberately not fixed** | `_token_overlap` measures the fraction of the *quote's* tokens found in the trigger text, so the shorter the quote the easier it is to satisfy: `"или"` passes against the sample transcript. A minimum quote length would close it, but the frozen metric definition says "does not appear verbatim", and tightening the rule after publishing the definition would make reported numbers incomparable to the metric they claim to be. Stated in README §11. Fix it in the next build, not mid-flight. |
| 29 | The gate verified cards citing a transcript segment or frame as a "representative message" | **Fixed 2026-08-30** | Evidence means chat messages; the gate accepted any event id, so a card could offer the streamer's own speech as the audience's response to it and pass with no violations. Two new codes, `E_EVIDENCE_NOT_A_MESSAGE` and `E_CIRCULAR_EVIDENCE`. All four frozen gold cards still pass, asserted by test. |
| 30 | The reducer silently deleted every emote-only message | **Fixed 2026-08-30** | `canonical` stripped `[^\w\s]+`, which removes symbols as well as punctuation, so `😂😂😂` canonicalised to nothing and `reduce_chat` dropped it. On Twitch that is the most common reaction there is, and the module's own docstring claims counts are preserved. Measured before the fix: 5 messages in, 4 lost, burst counts summing to 1. Punctuation is now stripped by Unicode category so symbols survive, and a punctuation-only message goes to a counted `∅` bucket instead of vanishing. Verified the scaffold fixture reduces identically — 32 → 15 bursts, ratio 0.469 — so nothing frozen moved. |
| 31 | Trigger accuracy — metric A — could not be lowered by emitting noise, and the README stated a definition the code did not implement | **Fixed 2026-08-30** | Measured: one correct card plus nine hallucinations reported trigger accuracy 1.0. The denominator is matched cards, not "the cards emitted" as README §6 claimed. The denominator is kept — gold is not exhaustive on twelve cases, so a real signal nobody labelled would otherwise score as a wrong trigger — but the README now says what is computed, and `unmatched_rate` is reported beside it and in `comparison.csv`. That probe now reads 1.0 / 0.9. Nothing had been measured yet, so the frozen protocol is intact. |
| 32 | The same gold signal could be matched by two cards | **Fixed 2026-08-30** | A system emitting one card twice had that single gold signal weighted twice in trigger accuracy. Matching is now one-to-one in emission order; the duplicate counts as unmatched. |
| 33 | The card cap existed only as a request in the prompt; neither system enforced it | **Fixed 2026-08-30** | Measured: a model returning ten cards had all ten kept, in the agent and in the baseline. `03-EVAL_DESIGN.md` promises the baseline "the same output schema and card cap", and an unenforced cap contaminates the comparison — a system that ignores it gets more chances at recall and more cards over which the unsupported rate is averaged. `cap_cards` now applies `MAX_CARDS = 3` in one place used by both, unknown types are dropped first so junk cannot fill the cap, and the number dropped is reported in the run document and the trajectory. |
| 34 | Tool calls beyond four per step were truncated without telling the model | **Fixed 2026-08-30** | To the model a silently dropped call is indistinguishable from a tool that returned nothing. The results payload now names how many calls were not executed and why. |
| 44 | Diary dates in `PROGRESS.md` and `DECISIONS.md` run a day ahead of the clock | **Open, disclosed 2026-08-30** | Real time at discovery was `2026-08-30 17:40 UTC`; 8 PROGRESS headings and 57 DECISIONS rows read `2026-08-31`, and two `COST_LEDGER.md` lines carry MSK times labelled `Z`. The cause is the same one already disclosed on the philosophy page — this repo's git history was reconstructed with assigned dates, and the assumption leaked into the working documents. **No measurement is affected**: every published number comes from the committed cache and the scorer, and every spend figure is computed from token counts inside cache entries, never from a timestamp. Not mass-rewritten with 24 hours to the deadline and the video unshot; disclosed here and in the ledger instead. |
| 45 | Entries above refer to the product as "Twitch Agent" | **Closed 2026-08-30** | Renamed to Twinky. This file is append-only, so earlier rows keep the former name deliberately rather than being rewritten. The rename touched no prompt, no fixture, no cache entry and no recorded artifact; `make eval` still reports 48 hits / 0 misses and `evidence/report.md` is byte-identical. |
| 46 | `.env.example` set a model name that breaks keyless replay | **Fixed 2026-08-30** | It declared `TS_TEXT_MODEL=deepseek-v4-flash` while every recorded response came from `gpt-4.1-nano`. The model name is part of the cache key, so copying the example to `.env` turned every entry into a miss and stopped `make eval` reproducing — verified by doing it. Model names are now commented out with the reason, and a test fails if either is ever set live. It also omitted `TS_LLM_BASE_URL`, without which an OpenAI key is sent to DeepSeek and returns 401 — the exact failure hit during a record phase this session. |
| 47 | `origin/main` is **70** commits behind local, and #37 says only "make it public" | **OPEN — P0, author action** | Found 2026-08-30 by comparing the remote to local rather than reading the risk register. `origin/main` is at `5ce881b`, commit 104; local is at 135. The remote is missing the grouping arm, the three-zone dashboard, the questions panel, live counts, Tier 0 live chat, the agent graph, the moderation panel, group labels, both the grounded-arm and embeddings measurements, the rename to Twinky, and every document correction. **Making the repository public without pushing first would publish the wrong project, and it would look finished** — which is worse than a private repository, because nobody would think to check. The build sessions are forbidden from pushing, so this is author action. `make preflight` fails while it stands. |
| 48 | Three incomplete captures appeared on disk mid-session | **Closed 2026-08-31 — committed and documented** | `hasanabi`, `yugi` and `zackrawrr`, all stamped `2026-08-30T2035`, each holding only `meta.json` and a gitignored `raw/` — **no `chat.jsonl`, no derived events**, so nothing in the codebase can read them. 52 MB total. Not created by the build sessions and not committed: they are incomplete, they are not part of the frozen eval, and `raw/` never ships. Left on disk untouched rather than deleted, because they are capture data and deleting someone else's work unasked is not this session's call. `make preflight` reports them as untracked captures and does not block on them.. **Closed 2026-08-31:** their `meta.json` files are now committed alongside nineteen more like them — 22 capture-only fixtures in total, 8 channels, 32,958 messages, 2.9 hours, 11 KB of metadata and no derived events. `evals/DATA.md` now accounts for every directory in `evals/fixtures/`, and a test holds that nothing but `meta.json` ships for any of them |
| 49 | `make scan` only ever looked at the working tree, while publishing exposes all history | **Fixed 2026-08-30** | A credential committed once and removed in the next commit stays in the pack, and no filename check finds it because the leak is in the content. Added `scan_secrets.py --history`: every version of every text file ever committed, 0.6 s for 2110 objects. **Result: this repository's history is clean** — the only pattern hits are the scanner's own synthetic test fixtures (`your_password`, `changeme`, `replace-me`, `localhost`), and `.env` and `.capture_salt` have never appeared in any commit. Verified to catch a real one by planting a key in a throwaway repo and deleting it in the next commit. Now a hard check in `make preflight`. |
| 50 | The product page's statistics rail was laid out as a two-column grid inside a ~259px column | **Fixed 2026-08-31** | `#rail` named two different components — the Method page's card rail, which wants `repeat(2, minmax(0, 1fr))` at ≥1100px, and the product page's statistics rail, which wants a stacked column. An id (1,0,0) beats `.panel-rail .rail` (0,2,0), so the id rule won wherever they disagreed and the rail's six blocks were squeezed into two ~110px columns whenever it was visible. Found by auditing breakpoints, not by looking at the page. The product rail is now `#window-rail`; `#rejected-rail` and `#abstained-rail`, styled but present in no page, were deleted. |
| 51 | `.env.example` was excluded from the archive, and `make test` failed inside it | **Fixed 2026-08-31** | `.gitattributes` carried `.env.* export-ignore` as defence-in-depth against `.env.local` and friends; it also matched `.env.example`, the one file in that family that must ship. Consequences: a judge who wanted to record had **no template at all**, the previous iteration's work making that template safe to copy never reached anyone, and six tests that read it failed — so `make test` was **red in the extracted archive** while green in the working tree. Invisible everywhere except inside the zip. Fixed with a later, more specific `-export-ignore` line; three tests now assert the template ships, the local-only files still do not, and every test file is present. |
| 52 | Four real stream frames with **unpseudonymised Twitch logins** ship in `video/twinky-image-bank.zip` | **OPEN — P1, author decision before publishing** | Found 2026-08-31 by measuring what a `git push` would carry, not by reading the tree. `bank/01-real/stream-frames/` holds four JPEGs from `stableronaldo_2026-08-30T0723` with the chat overlay visible, logins included. Every chatter in every fixture is pseudonymised and `README.md`, `evals/DATA.md` and each `meta.json` say so, and `video/SHOTLIST.md` shot 3 claimed frames "are not in the repo or the archive" — which was false and is now corrected. The bank's own `MANIFEST.md` carries the warning, inside the zip, where nobody reads it. Three options, all the author's: blur the username column and keep the frames, drop `01-real/` from the zip, or drop the zip. Whichever, **the unblurred frame must not go on camera** — it shows precisely what the pipeline is documented as not storing. Nothing was deleted here: it is the author's video material and shot 3 depends on the hero frame. |
